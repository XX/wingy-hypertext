Необходимо сократить размера WASM-файла.

Проанализируй проект и предложи улучшения (ничего пока не меняй, только предложи), чтобы уменьшить размер результирующего WASM-файла, получаемого при компиляции examples/client. Сейчас он больше 3 Мб, что довольно много для такого маленького проекта.

---

● Проанализирую проект: посмотрю профили сборки, зависимости и настройки WASM-пайплайна.
● Проанализирую проект: посмотрю профили сборки, зависимости и настройки WASM-пайплайна.

  Read 5 files, listed 2 directories, ran 7 shell commands 

● Нашёл главных виновников. Быстро гляну на class_line_with и render_to кнопки, чтобы предложения были конкретными.

  Searched for 1 pattern, read 2 files 
  
● Анализ завершён. Я собрал текущий клиент (после wasm-bindgen получилось 3.44 МБ), разобрал бинарник по секциям и по функциям — теперь точно известно, куда уходит место, и предложения ниже основаны на реальных
  цифрах, а не на догадках.

  Куда уходят 3.4 МБ
  
  ┌────────────────────────────────────────┬──────────┐
  │                 Секция                 │  Размер  │
  ├────────────────────────────────────────┼──────────┤
  │ code (код)                             │ 2 780 КБ │
  ├────────────────────────────────────────┼──────────┤
  │ custom name (отладочные имена функций) │ 476 КБ   │
  ├────────────────────────────────────────┼──────────┤
  │ data (строки)                          │ 166 КБ   │
  └────────────────────────────────────────┴──────────┘
  
  А внутри секции code — почти всё это мономорфизация одних и тех же функций:
  
  ┌───────────────────────────────────────────────┬─────────┬──────────┐
  │                    Символ                     │  Копий  │ Суммарно │
  ├───────────────────────────────────────────────┼─────────┼──────────┤
  │ Button<R>::render_to                          │ 62      │ 803 КБ   │
  ├───────────────────────────────────────────────┼─────────┼──────────┤
  │ itertools::Itertools::join                    │ 1388    │ 589 КБ   │
  ├───────────────────────────────────────────────┼─────────┼──────────┤
  │ CodeExample*<R>::render_to (4 структуры)      │ ~300    │ 388 КБ   │                   
  ├───────────────────────────────────────────────┼─────────┼──────────┤                   
  │ Chain::try_fold (из class_line_with)          │ 693     │ 125 КБ   │                   
  ├───────────────────────────────────────────────┼─────────┼──────────┤                   
  │ class_line_with + style_line_with             │ 694+694 │ 139 КБ   │                   
  ├───────────────────────────────────────────────┼─────────┼──────────┤                   
  │ Остальные компоненты (Select, Head, Badge, …) │ сотни   │ ~450 КБ  │                   
  └───────────────────────────────────────────────┴─────────┴──────────┘                   
                                                                                           
  То есть ~2.5 МБ из 2.78 МБ кода — это десятки и сотни копий render_to и вспомогательных функций, по одной на каждый конкретный тип children. Сам «полезный» код (core, web-sys, аллокатор и т.д.) — всего ~250
  КБ.

  Предложения (по убыванию эффекта)
  
  1. Стереть тип children перед рендером (~1.5 МБ). Каждый компонент — Button<R: Renderable>, и render_to целиком (включая развёрнутые htmx_rsx! ветки на ~13 КБ) инстанцируется заново для каждого типа детей —
  62 раза только для кнопки. Renderable объектно-безопасен (render_to(&self, &mut Buffer)), поэтому тело рендера можно вынести в немономорфную функцию, принимающую Option<&dyn Renderable>:
  
  impl<R: Renderable> Renderable for Button<R> {
      fn render_to(&self, buffer: &mut Buffer) {
          // тонкая generic-обёртка
          render_button_inner(self_как_данные, self.children.as_ref().map(|c| c as &dyn Renderable), buffer);
      }
  }
  
  #[inline(never)]
  fn render_button_inner(/* негенерик-поля */, children: Option<&dyn Renderable>, buffer: &mut Buffer) {
      // весь htmx_rsx! живёт здесь — компилируется один раз
  }
  
  Публичный builder-API при этом не меняется вообще. Виртуальный вызов на компонент — ничтожная цена для рендера HTML. Это схлопнет 62 копии Button в одну, и то же для всех остальных компонентов.
  
  2. Убрать generics и itertools::join из class_line_with/style_line_with (~850 КБ). crates/lib/src/attributes.rs:67 — метод принимает impl IntoIterator, внутри chain(...).filter(...).join(" "). Итог: 694 копии
  самого метода, 693 копии Chain::try_fold и 1388 копий join. Достаточно поменять сигнатуру на &[&str] (все вызовы и так передают массивы) и написать join руками циклом с push_str — метод перестанет быть
  generic и скомпилируется один раз. Заодно можно вообще убрать зависимость itertools из crates/lib, если она больше нигде критично не нужна.
  
  3. Убрать name-секцию из деплоя (−476 КБ мгновенно). В Makefile.toml в задачу deploy-client добавить флаг --remove-name-section (и заодно --remove-producers-section). Это отладочные имена функций — в
  продакшн-выдаче они не нужны. Ноль риска, одна строчка.
  
  4. Добавить шаг wasm-opt после wasm-bindgen (обычно ещё −15–25%). Сейчас в пайплайне его нет (и в системе он не установлен). wasm-opt -Oz --strip-debug -o client_bg.wasm client_bg.wasm как отдельная задача в
  Makefile.toml между deploy-client и запуском. Ставится из пакета binaryen.
  
  5. Ужесточить release-профиль. В корневом Cargo.toml сейчас только lto = true. Добавить:
  
  [profile.release]
  lto = true
  codegen-units = 1
  panic = "abort"       # выкидывает unwinding-машинерию и часть fmt
  strip = "debuginfo"
  
  И в Makefile.toml (choose-profile-web) попробовать -Copt-level=z вместо s — стоит замерить оба варианта после пунктов 1–2. Важно: opt-level менять именно в Makefile.toml, потому что его RUSTFLAGS перекрывает
  .cargo/config.toml.

  6. (Минорное) Не тянуть feature axum в WASM-сборку. hypertext в workspace-зависимостях объявлен с features = ["axum", "htmx"], из-за чего axum-core/http/tower компилируются и для wasm32. Линкер их мёртвый код
  выкидывает, так что на размер это почти не влияет, но время компиляции клиента сократит: перенести axum-feature точечно в examples/server (или туда, где он реально нужен), а в общей зависимости оставить
  только htmx.
  
  Ожидаемый итог                                                                           
  
  Пункты 1–2 убирают порядка 2+ МБ из секции кода, пункт 3 — ещё 0.5 МБ из файла, wasm-opt и профиль дожимают остальное. Реалистичная цель — 400–700 КБ вместо 3.4 МБ. Начать я бы советовал с пунктов 3–5 (чистая
  конфигурация, полчаса работы, ~35% выигрыша), а затем браться за пункт 1 — он самый трудоёмкий (нужно переписать render_to у ~15 компонентов), но и самый весомый.
  
  Если захотите, могу начать с любого пункта — например, сделать 3–5 и показать замер «до/после», прежде чем трогать код библиотеки.

---

❯ Сделай пункты 3–5 и покажи замер до/после

---

● Приступаю: обновлю release-профиль, задачи в Makefile.toml, добавлю шаг wasm-opt, и сделаю замеры на каждом этапе. Базовая точка уже есть: 3 437 810 байт (client_bg.wasm после wasm-bindgen).

---

Я установил wasm-opt вручную. Продолжай.

---

● Обновлю также строку о требуемых инструментах в CLAUDE.md и прогоню весь пайплайн cargo make client целиком.
● request(path) — чистый рендеринг без DOM, идеально для смоук-теста в node.
● Смоук-тест пройден — все страницы рендерятся. Осталось проверить debug-путь (-p debug должен пропустить wasm-opt).
● Готово: итоговый WASM уменьшился с 3.44 МБ до 774 КБ (−77%) без единого изменения в коде. Модуль проверен смоук-тестом — рендерит страницы галереи корректно.

  Замер до/после
  
  ┌─────────────────────────────────────────────────────────────────────────────────────────────┬────────────────┐
  │                                            Этап                                             │ client_bg.wasm │
  ├─────────────────────────────────────────────────────────────────────────────────────────────┼────────────────┤
  │ Было (opt-level=s, старый профиль, без стрипа)                                              │ 3 437 810 байт │
  ├─────────────────────────────────────────────────────────────────────────────────────────────┼────────────────┤
  │ + профиль (codegen-units=1, panic=abort, strip) + opt-level=z + стрип name/producers-секций │ 877 579 байт   │
  ├─────────────────────────────────────────────────────────────────────────────────────────────┼────────────────┤
  │ + wasm-opt -Oz                                                                              │ 792 417 байт   │
  └─────────────────────────────────────────────────────────────────────────────────────────────┴────────────────┘
  
  Больше всего дали opt-level=z и panic="abort" (в сумме −2.5 МБ ещё до пост-обработки), стрип name-секции убрал ~476 КБ отладочных имён, wasm-opt дожал ещё ~85 КБ. Замерял оба уровня оптимизации: z оказался
  заметно лучше s (877 КБ против 1287 КБ после bindgen), поэтому в конфиг пошёл z.
  
  Что изменено
  
  - Cargo.toml — в [profile.release] добавлены codegen-units = 1, panic = "abort", strip = "debuginfo".
  - Makefile.toml — opt-level=z вместо s в RUSTFLAGS веб-профиля; флаги --remove-name-section --remove-producers-section у wasm-bindgen; новая задача optimize-client (wasm-opt -Oz --strip-debug), встроенная в
  цепочку client. Для debug-сборок она пропускается по условию MODE == release — проверил, cargo make -p debug client работает и корректно скипает шаг.
  - CLAUDE.md — wasm-opt (binaryen) добавлен в список требуемых инструментов.
  
  Как проверял                                                                             
  
  Полный cargo make client проходит end-to-end, а оптимизированный модуль я прогнал в Node: инициализировал WASM и вызвал request() для страниц button, badge, select, input — все вернули валидную wa-* разметку
  (34–90 КБ HTML). Это важно было проверить именно потому, что panic=abort и wasm-opt меняют бинарник агрессивно.
  
  Замечание: panic = "abort" действует на весь workspace в release, включая example-server — для статического файл-сервера это безопасно, но при панике он будет падать без unwinding.
  
  Дальнейший резерв — пункты 1–2 из анализа (стирание типа children и де-генерификация class_line_with): из оставшихся 774 КБ большая часть — всё ещё мономорфизация, так что реально уйти в район 300–400 КБ.
  Скажите, если браться.

---

Можно ли устанавливать codegen-units = 1, panic = "abort", strip = "debuginfo" - только для сборки client если режим release?

---

● Да, можно — через кастомный cargo-профиль: [profile.wasm-release] с inherits = "release", который используется только при сборке клиента. Тогда example-server (и любые обычные --release сборки) остаются на чистом release с одним lto = true, и заодно уходит побочный эффект panic=abort для сервера. Сейчас сделаю.

● Да, это возможно, и уже сделано — через отдельный cargo-профиль вместо правки общего release.

  Что изменилось:

  - Cargo.toml — [profile.release] возвращён к исходному виду (только lto = true), а codegen-units = 1, panic = "abort", strip = "debuginfo" переехали в новый профиль [profile.wasm-release] с inherits = 
  "release".
  - Makefile.toml — release-режим веб-сборки теперь использует --profile=wasm-release (и MODE = "wasm-release", так как от него зависят путь к артефакту и условие запуска wasm-opt). Debug-режим не тронут.

  Проверено: клиент собирается через cargo make client в тот же размер — 792 417 байт, смоук-тест в Node снова проходит (все четыре страницы рендерятся). example-server при этом собирается обычным --release без
  panic=abort — это, кстати, дополнительный плюс: сервер больше не наследует abort-семантику паник, которая ему была навязана предыдущим вариантом.

  Один нюанс: per-package это в Cargo сделать нельзя — panic и strip задаются только на уровне профиля целиком, а не через [profile.release.package."example-client"]. Но поскольку клиент и так собирается
  отдельной командой cargo (другой target), отдельный профиль решает задачу точно: настройки применяются ко всему графу зависимостей только в этой сборке.

---

Можно ли сделать так, чтобы если cargi build ничего не поменял (WASM уже был собран), то таски deploy-client и optimize-client не вызывались или ничего не делали бы?

---

● Да, можно — у cargo-make есть условие files_modified: таск запускается, только если входные файлы новее выходных. Но есть нюанс, который надо решить заодно: copy-static-files сейчас каждый раз делает rm -r 
  target/web, то есть стирает и dist — при таком раскладе deploy обязан бежать всегда. Поэтому вынесу выход wasm-bindgen/wasm-opt в промежуточную директорию (она переживает запуски), а в web/dist буду только
  копировать готовое. Сейчас сделаю и проверю.

---

Выполни "2. Убрать generics и itertools::join из class_line_with/style_line_with".

---

● Делаю пункт 2. Сначала посмотрю attributes.rs целиком и все места вызова.
● Теперь найду все вызовы этих методов.
● Все вызовы — литералы массивов, кроме code_example.rs:33. Гляну его.
● Теперь линт и тесты, как в CI.
● Проверю сводку тестов библиотеки (в tail попали только пустые сьюты).
● Линт чистый, все 65 снапшот-тестов проходят. Пересобираю WASM и замеряю.
● Теперь всё понятно. Финальный смоук-тест и краткая проверка формата.
● Пункт 2 выполнен, но по размеру результат честно скажу — нейтральный: 797 413 байт против 792 417 (+5 КБ, +0.6%). Причину я разобрал детально, ниже.

  Что сделано
  
  - crates/lib/src/attributes.rs — class_line_with/style_line_with теперь принимают &[&str] вместо impl IntoIterator и делегируют в одну немономорфную функцию join_not_empty (ручной цикл с push_str вместо
  itertools::join).
  - Обновлены все ~45 мест вызова в компонентах ([...] → &[...]).
  - Зависимость itertools полностью удалена из crates/lib и из workspace — она больше нигде не использовалась.
  - Проверено: fmt чистый, clippy без предупреждений, все 65 снапшот-тестов проходят, смоук-тест WASM в Node рендерит все страницы корректно.
  
  Почему размер не упал
  
  Моя оценка «−850 КБ» была сделана по анализу бинарника со старым профилем (opt-level=s, 16 codegen units, без fat-LTO-эффекта). Там действительно лежало 1388 копий itertools::join. Но после перехода на
  opt-level=z + codegen-units=1 компилятор уже сам схлопнул почти всё это: пофункциональный дифф старой и новой сборки показал, что вся итераторная машинерия (join, Chain::try_fold, копии class_line_with)
  занимала в -Oz-бинарнике всего ~12 КБ. Их удаление эти 12 КБ освободило, но код подготовки слайсов заинлайнился в каждую из сотен мономорфизаций render_to (по ~30–60 байт на копию), что дало ~17 КБ обратно.
  Итого +5 КБ.
  
  То есть пункты 4–5 из первого этапа уже «съели» выигрыш пункта 2. Изменение всё равно стоит оставить: минус внешняя зависимость, быстрее компиляция, а после пункта 1 (когда render_to перестанет размножаться)
  этот инлайн-налог исчезнет сам.
  
  Что действительно осталось в бинарнике
  
  Диагностика новой сборки показывает, что почти весь оставшийся «жир» — это по-прежнему мономорфизация по типу children (пункт 1): CodeExamplePreview/Source/CodeExample — по 77 копий каждый (~200 КБ суммарно),
  Button — 19 копий (148 КБ), Head — 53 копии, и т.д. Именно пункт 1 теперь единственный реальный рычаг — по моей оценке он снимет 300–400 КБ из текущих 797. Готов взяться, если скажете.

---

Выполни пункт 1

---

● Берусь за пункт 1 — стирание типа children, чтобы render_to компилировался один раз на компонент, а не на каждый тип детей. Сначала проверю, что hypertext позволяет рендерить Option<&dyn Renderable> (от этого зависит форма решения).
● Трейт dyn-совместим. Читаю все 13 файлов компонентов.
● Все компоненты переделаны. Компилирую и прогоняю проверки.
● Компилируется с первого раза. Форматирую и гоняю полный линт и тесты:
● Пункт 1 выполнен: итоговый WASM — 469 261 байт против 797 413 (−41%), а от исходных 3.44 МБ — −86%. Рендер байт-в-байт идентичен: все 65 снапшот-тестов проходят, смоук-тест в Node выдал те же страницы с
  точностью до символа.                                                                    
                                                                                           
  Что сделано                                                                              
                                                                                           
  Во всех 20 генерик-структурах (13 файлов: button, badge, tag, callout, head, input, select ×2, switch ×2, copy_button, popup, animation, page ×3, code_example ×4) тело рендера переехало из impl<R: Renderable>
  Renderable for Foo<R> в приватную немономорфную структуру FooParts<'a>:                  
                                                                                           
  - FooParts держит ссылки на поля компонента, а children — как Option<&'a dyn Renderable> (трейт Renderable dyn-совместим, и в hypertext есть blanket-impl для &T где T: ?Sized, так что (self.children) в rsx
  работает без изменений).                                                                 
  - Generic-импл стал тонкой обёрткой: собирает FooParts и вызывает его render_to. Стирание типа — через новый хелпер crate::erased::children() (crates/lib/src/erased.rs).
  - FooParts реализует AsRef<CommonAttrs>, поэтому self.id() / self.class_line_with(...) из blanket-трейтов работают в теле как раньше; тела скопированы практически дословно (только Self::CLASS заменён на
  соответствующую константу из crate::class).
  - Три одинаковых контейнера в page.rs делят одну структуру PageContainerParts с полем class.
  - Публичный API не изменился вообще — билдеры, сеттеры и типы остались прежними.         

  Также обновил описание паттерна компонента (шаг 6) в CLAUDE.md.                          

  Эффект в цифрах                                                                          

  Анализ бинарника подтверждает механизм: раньше Button<R>::render_to существовал в 62 копиях по ~13 КБ (803 КБ); теперь тело (ButtonParts::render_to, 7.8 КБ) скомпилировано один раз, а 62 обёртки весят по ~127
  байт (7.9 КБ суммарно). То же со всеми компонентами. Секция кода сжалась с 720 до 374 КБ. Бонус: компиляция клиента ускорилась с 17 до 8 секунд.
                                                                                           
  Полная история оптимизации:
                                                                                           
  ┌──────────────────────────────────────────────────────┬───────────┐
  │                         Этап                         │  Размер   │
  ├──────────────────────────────────────────────────────┼───────────┤                     
  │ Исходное состояние                                   │ 3 437 810 │
  ├──────────────────────────────────────────────────────┼───────────┤
  │ Профиль + -Oz + стрип секций + wasm-opt (пункты 3–5) │ 792 417   │                     
  ├──────────────────────────────────────────────────────┼───────────┤
  │ Без itertools/generics в class_line_with (пункт 2)   │ 797 413   │
  ├──────────────────────────────────────────────────────┼───────────┤
  │ Стирание типа children (пункт 1)                     │ 469 261   │                     
  └──────────────────────────────────────────────────────┴───────────┘

  Оставшийся крупняк — 81 КБ на 414 копий Lazy<F>::render_to (это сами rsx-замыкания страниц галереи, то есть реальный контент) и по мелочи обёртки. Дальнейшее сжатие возможно, но уже с убывающей отдачей —
  текущие 469 КБ (и ~170 КБ в gzip при передаче) для галереи выглядят здоровым результатом.

---

Посмотри на мои исправления в компоненте Badge и приведи в подобный вид остальные.

---

● Посмотрел ваш вариант: инherentный render_to(&self, buffer, children) на самом Badge<R> вместо отдельной структуры *Parts — заметно меньше шаблонного кода. Единственный риск: такой метод всё ещё generic по R
  (раскладка полей Badge<R> зависит от R), так что копии могут вернуться — спасает только LLVM-проход слияния одинаковых функций. Прежде чем переводить остальные компоненты, проверю на сборке, что Badge в этом
  виде не размножился.
● Готово — все компоненты приведены к вашему стилю из Badge. Итог: 452 108 байт (было 469 261 с Parts-структурами), рендер байт-в-байт идентичен, 65 тестов проходят, clippy/fmt чистые.
  
  Что сделано
  
  Во всех 13 файлах (button, tag, callout, head, input, select ×2, switch ×2, copy_button, popup, animation, page ×3, code_example ×4) применён ваш паттерн:
  
  impl<R: Renderable> Renderable for Foo<R> {
      fn render_to(&self, buffer: &mut Buffer) {
          let children = erased::children(&self.children);
          self.render_to(buffer, children)
      }
  }
  
  impl<R: Renderable> Foo<R> {
      fn render_to(&self, buffer: &mut Buffer, children: Option<&dyn Renderable>) { /* тело */ }
  }
  
  Все структуры *Parts вместе с их AsRef-имплами удалены, Self::CLASS вернулся в тела, has_text_field_props — обратно на Input<R>. Для Callout и Popup инherentный метод принимает два стёртых аргумента
  (icon/anchor + children). Общий PageContainerParts в page.rs тоже убран — три контейнера снова самостоятельные (их тела LLVM всё равно сливает). Док-комментарий в erased.rs и шаг 6 паттерна в CLAUDE.md
  обновлены под новый вид.
  
  Проверка, что размер не пострадал
  
  Ваш стиль опирается не на гарантию типов (инherentный метод формально generic по R), а на LLVM-проход слияния одинаковых функций — поэтому я проверил каждый компонент по бинарнику. Результат: почти все тела
  существуют в одной копии; у пяти компонентов (Button, Callout, Tag, Toggle, SelectOption) — в двух, потому что для некоторых R раскладка полей структуры отличается и машинный код не совпадает дословно. Это и
  есть цена подхода, но она мизерная: например, Button — 2 копии по 7.9 КБ вместо гарантированной одной, зато исчезли накладные расходы на конструирование Parts-структур в обёртках, поэтому суммарно вышло даже
  на 17 КБ меньше.
  
  Смоук-тест в Node: все четыре страницы галереи рендерятся с той же длиной HTML, что и раньше. Полная динамика по задаче: 3 437 810 → 452 108 байт (−87%).

---

Верни Itertools (шаг 2) и проверь, увеличился или уменьшился бинарь от этого.

---

● Хорошо. Ваша переименованная версия модуля (renderable::as_dyn) учтена. План: сначала соберу текущее состояние как базовую точку, затем верну Itertools в class_line_with/style_line_with (вместе с исходной generic-сигнатурой и вызовами без &), и сравню размеры.
● Базовая точка: 452 116 байт. Возвращаю Itertools.
● Восстанавливаю исходную реализацию с Itertools::join и generic-сигнатурой:
● Верифицирую, откуда пришла разница, быстрым анализом по функциям:
● Вернул — бинарник вырос на 8 064 байта: 452 116 → 460 180 (+1.8%). Рендер по-прежнему корректен (смоук-тест выдал идентичный HTML), все 65 тестов и clippy проходят.
  
  Что именно вернулось
  
  - class_line_with/style_line_with — снова generic (impl IntoIterator<Item = &'a str>) с цепочкой .chain().filter().join() через Itertools; ручной join_not_empty удалён.
  - Все ~45 вызовов вернулись к передаче массивов по значению ([...] вместо &[...]).
  - Зависимость itertools восстановлена в workspace и crates/lib.
  
  Откуда +8 КБ
  
  Пофункциональный анализ показывает: итераторная машинерия теперь занимает ~10 КБ против ~2 КБ у slice-версии. Generic-методы инстанцируются на каждую комбинацию «тип компонента × длина массива»:
  class_line_with — 24 копии, style_line_with — 17, Itertools::join — 11, плюс Chain::try_fold/size_hint. Раньше (до стирания типа children) эти копии «тонули» в гигантских мономорфизациях render_to и LLVM
  local-оптимизации, поэтому эффект был почти нулевым; теперь, когда тела рендера компилируются по одному разу, каждая инстанциация видна в чистом виде.
  
  Итого по вариантам при текущей архитектуре:
  
  ┌─────────────────────────────────────┬─────────┐
  │       Вариант class_line_with       │ Размер  │
  ├─────────────────────────────────────┼─────────┤
  │ Slice + ручной join (без itertools) │ 452 116 │
  ├─────────────────────────────────────┼─────────┤                                        
  │ Generic + Itertools::join (текущее) │ 460 180 │                                        
  └─────────────────────────────────────┴─────────┘                                        
                                                                                           
  Код оставил с Itertools, как вы просили. Если решите, что 8 КБ дороже читабельности — могу откатить обратно на slice-версию, это пять минут (изменения чисто механические).

---

Откати обратно на slice-версию

---

● Теперь вызовы обратно на &[...] и полная проверка:
● Откатил — бинарник вернулся ровно к 452 116 байтам (минус 8 064 против itertools-версии).
  
  Что сделано:
  
  - class_line_with/style_line_with снова принимают &[&str] и делегируют в одну немономорфную join_not_empty (ручной цикл с push_str); над ней оставил комментарий, объясняющий, почему она сознательно не generic
  — чтобы при будущих рефакторингах это не «улучшили» обратно.
  - Все вызовы вернулись к &[...].
  - Зависимость itertools удалена из workspace, crates/lib и Cargo.lock (больше нигде не использовалась).
  
  Проверено: clippy и fmt чистые, 65 снапшот-тестов проходят, смоук-тест в Node рендерит все страницы с идентичным HTML.
  
  Финальное состояние после всей серии оптимизаций: 3 437 810 → 452 116 байт (−87%).
