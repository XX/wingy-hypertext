Также, как были реализованы компоненты Badge, Button и другие, необходимо реализовать компонент `Tag`, повторяя функционал компонента `wa-tag` из WebAwesome: `tmp/webawesome` (variant, appearance, pill, кнопка удаления `with-remove`). Активация кнопки удаления должна порождать всплывающее событие `wa-remove` на элементе тега (сам тег при этом себя не удаляет — обработку решает потребитель); логику реализовать на Rust в `crates/web`. Поддержка удаления пригодится в дальнейшем для отображения выбранных опций бейджами в `Select` с `multiple`. Для визуального тестирования необходим также новый раздел Tag с примерами использования в `examples/client`.

---

● Задача выполнена: issue заведён (issues/tag component.md) и реализован. Все проверки прошли: fmt, clippy (-D warnings), 60 тестов (5 новых для Tag), WASM-клиент собран.
  
  Компонент (crates/lib/src/components/tag.rs) — порт wa-tag: пропы variant, appearance, pill, with_remove + стандартные CommonAttrs/children. Рендерится как <span class="tag …"><span 
  class="tag-content">…</span>[кнопка]</span>; при with_remove добавляется <button class="tag-remove" aria-label="Remove"> с крестиком (inline SVG, как в copy_button). Дефолты — как у WA: Neutral +
  FilledOutlined (у appearance это потребовало ручного impl Default, по прецеденту Callout). Размеры — через size-* классы, как у Badge.
  
  Удаление (crates/web/src/components/tag.rs) — точная семантика wa-tag: клик по кнопке порождает всплывающее событие wa-remove на элементе тега, сам тег себя не удаляет — обработку решает потребитель. Имя
  события экспортировано константой REMOVE_EVENT, а dispatch_remove_tag публичен — это задел для Select: его обработчик тегов сможет и слушать wa-remove от своих чипов, и переиспользовать разметку/классы.
  Подключается одним вызовом listen_remove_tags() (делегированный слушатель).
  
  CSS (webassets/style/components/tag.css) — порт tag.styles.ts: базовый вид (высота 0.8 × form-control-height, gap 0.5em), все четыре appearance-модификатора, уменьшенный шрифт контента, кнопка удаления
  1em×1em с hover/active через --wa-color-mix-*, pill.
  
  Галерея — раздел Tag (маршрут, пункт меню после Switch): базовый пример, Variants, Appearance, Size, Pill и Removable. Removable повторяет демо из доки WA: слушатель wa-remove в контейнере .tags-removable
  гасит тег (opacity: 0 с transition) и возвращает через 2 секунды (dom::set_timeout), логика — в том же файле галереи (listen_tag_removable_demo).
  
  Теперь для тегов-бейджей в Select multiple всё готово: остались контейнер .tags в комбобоксе, генерация тегов в selection_changed по разметке Tag, проп max_options_visible и CSS-блок :host([multiple]) — могу
  заводить и выполнять следующую задачу, когда скажете.

---

Можно ли в крейте web для динамического создания Tag использовать компонент из крейта lib?

---

● Да, можно — и это лучше текущего решения: сейчас разметка тега в crates/web продублирована руками (константа с кнопкой-крестиком + сборка span'ов), и при изменении компонента Tag они разъедутся. Библиотечный
  крейт уже компилируется под wasm (его использует example-client), циклической зависимости нет (lib ни от чего не зависит), так что wingy-hypertext-web может зависеть от wingy-hypertext, отрендерить Tag в
  строку и материализовать её в DOM. Единственный нюанс — data-value: у Tag нет такого пропа (в WA его тоже ставит сам select поверх wa-tag), поэтому атрибут навешивается после создания элемента. Реализую:

● Да, можно — и сделано. Все проверки прошли: fmt, clippy (-D warnings), 61 тест, WASM-клиент собран.
  
  Что изменилось. Теперь crates/web создаёт теги, рендеря сам библиотечный компонент: Tag::builder().pill(...).with_remove(...).children(label).render() → полученная разметка материализуется через временный
  контейнер (set_inner_html + first_element_child), после чего сверху навешивается data-value — ровно как wa-select ставит data-value на wa-tag в своём getTag-шаблоне. Ручная копия разметки (константа с
  кнопкой-крестиком и посборочное создание span'ов) удалена — единственный источник правды о разметке тега теперь crates/lib/src/components/tag.rs, и любые его будущие изменения автоматически подхватятся
  динамическими тегами. Экранирование подписей по-прежнему безопасно: children(label) проходит через hypertext-рендер, который экранирует текст.
  
  Почему это архитектурно нормально:
  - Направление зависимости чистое: wingy-hypertext-web → wingy-hypertext (библиотека ни от чего не зависит, цикла нет); в Cargo.toml web-крейта добавлены wingy-hypertext и hypertext (для
  RenderableExt::render).
  - Библиотека заведомо компилируется под wasm — example-client использует её так с самого начала.
  - Контракт между крейтами и так существовал (web завязан на имена классов и структуру разметки lib); теперь часть этого контракта выражена кодом, а не продублированной строкой.
  
  Побочный бонус: этот приём (render() компонента + вставка в DOM) — готовый паттерн для будущих компонентов, которым нужно достраивать разметку на клиенте.

---

Проверь, пожалуйста: в примере для Tag Removable кнопка с иконкой Xmark меняет свой размер вместе с размером тега, но в оригинальном примере WebAwesome иконка размер не меняет.

---

● Проверю: посмотрю задачу в issues/tag component.md, наш код тега с removable-кнопкой и сравню с оригинальным поведением Web Awesome.
● Теперь посмотрю пример Removable в доке WA и наш пример в галерее.
● Посмотрю, как wa-button в WA задаёт размер иконки внутри себя — это ключ к тому, почему в оригинале крестик не масштабируется.
● Проверил — наше поведение правильное, а расхождение с сайтом объясняется тем, что живой пример на webawesome.com просто отстаёт от исходников.
 
  Что выяснилось в tmp/webawesome:
 
  - До 9 июля 2026 в wa-tag кнопка удаления рендерилась как <wa-button> без атрибута size. У wa-button дефолт size='m' с reflect: true (button.ts:77), поэтому кнопка всегда получала font-size: 
  var(--wa-font-size-m) из общего size.styles.ts — и крестик оставался одного размера независимо от размера тега. Именно это поведение видено в живом примере на сайте.
  - В WebAwesome это считали багом и починили: коммит b6befe61f «Match wa-tag remove button to tag's size (#2615)» добавляет size=${this.size} кнопке, а в changelog записано «Fixed the remove button in <wa-tag>
  to match the tag's size». Этот коммит уже входит в наш снапшот tmp/webawesome, из которого портировался компонент.
 
  Наша реализация (.tag-remove { font-size: inherit } в webassets/style/components/tag.css) масштабирует крестик вместе с тегом — то есть воспроизводит именно исправленное, актуальное поведение WA-исходников.
  Опубликованный пример на сайте собран из версии до фикса, поэтому там иконка не меняется.

  Вывод: менять ничего не нужно — расхождение не в нашу сторону, а в сторону устаревшего живого примера. Если всё же хочется воспроизводить старое поведение (фиксированный крестик --wa-font-size-m), это одна
  строка в CSS — но я бы оставил как есть.

---
