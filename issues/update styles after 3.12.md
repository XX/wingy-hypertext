Просмотри свежую версию Webawesome в tmp/webawesome и проанализируй, какие понадобятся обновления стилей в webassets/style и какие изменения они потребуют в коде, чтобы привести его в соответствие с новой версией Webawesome. При этом обновление не должно ломать собственные разработки данного проекта, выведи также список подобных спорных мест, если они имеются.

---

● Сверка с Web Awesome 3.12.0 выполнена.

## Что с чем сравнивалось

`tmp/webawesome` — это `v3.12.0` + 12 коммитов ветки `next` (`git describe` → `v3.12.0-12-gf1981f73d`).
Точка последней синхронизации проекта — примерно `0ed6b6640` (2026-07-10): именно от неё расходятся
`webassets/style/common/**`, и она совпадает с датами файлов (`button.css`, `input.css`, `switch.css` — 11–12 июля).

Важно для оценки объёма работ: `webassets/style/common/**` — почти дословная копия `src/styles/**`
(отличий всего четыре, и три из них намеренные), а вот `components/`, `layouts/`, `helpers/` — не копии,
а самостоятельные адаптации под light DOM. Поэтому «обновление до 3.12» — это не мёрдж, а точечный перенос
конкретных фиксов.

Всего upstream за этот период тронул стили в 19 файлах; из них к проекту относятся 9.

## 1. Прямая синхронизация — безопасно, только CSS

1. **`common/utilities/visually-hidden.css`** (upstream #2765): добавить `top: 0px !important; left: 0px !important;`
   в блок `.wa-visually-hidden:not(:focus-within), …`. Класс реально используется — `crates/lib/src/component/head.rs:43`.
2. **`common/utilities/variants.css`** (#2678): в селектор `:where(:root), .wa-neutral` добавить `.wa-invert`,
   чтобы нативные элементы подхватывали инвертированные цвета.
3. **`common/native.css`** (#2729): перенести `font-size: var(--wa-font-size-smaller)` с общего правила `th`
   внутрь вложенного `&:not([scope='row']):not(:has(+ td))` — иначе `<th scope="row">` мельче соседних ячеек
   и его текст выпадает из вертикального выравнивания (падинги там em-относительные).

Изменений в Rust/JS не требуют.

## 2. Требуют правки CSS, код проверен и менять не нужно

4. **`components/switch.css`** (#2749): фокус-ринг переехал с ползунка на дорожку.
   `.switch-toggle .control:focus-visible:not(:disabled) ~ .track .thumb` → `… ~ .track`.
   Разметка (`switch.rs:135–136`, `.track > .thumb`) для этого уже подходит.
5. **`components/tooltip.css`** (#2638): стрелка рисуется не бордером, а внутренней тенью — в Safari
   `clip-path` вдоль бордера даёт светлый шов. Заменить
   `border-right/border-bottom: var(--wa-tooltip-border-width) var(--wa-tooltip-border-style) var(--wa-tooltip-border-color)`
   на `box-shadow: inset calc(-1 * var(--wa-tooltip-border-width)) calc(-1 * var(--wa-tooltip-border-width)) 0 0 var(--wa-tooltip-border-color)`.
   Геометрия стрелки в `helpers/popup.css` (clip-path, `--arrow-base-offset`) совпадает с upstream, так что
   фикс переносится один в один. Побочный эффект тот же, что у upstream: `--wa-tooltip-border-style` перестаёт
   влиять на стрелку (во всех темах там `solid`, визуально ничего не меняется).
6. **`layouts/page.css`** (#2690): upstream добавил хедеру `background-color: var(--wa-color-surface-default)` —
   **у нас это уже есть**, делать нечего.
7. **`utils/icon.css`** (#2677): upstream убрал `fill: currentColor` из правила `svg`, потому что CSS-правило
   перебивает presentation-атрибуты SVG. У нас его и не было, а `iconic` проставляет `fill` атрибутом на каждом
   `path` (`iconic/src/hypertext.rs:14`) — по этому пункту мы уже совпадаем с 3.12.
   **Но** рядом обнаружился свой баг: правило `svg { height: 1em; overflow: visible; width: auto; }`
   не отскоплено и применяется ко всем `<svg>` на странице. Должно быть `.icon svg`.

## 3. Новая функциональность 3.12 — можно подтянуть, но это не стилевая задача

8. **`DropdownItem` со ссылками** (#2733: `href`/`target`/`rel`/`download`). CSS-часть тривиальна
   (upstream добавил `#link { display: none }`), но основная работа в Rust и WASM: подмешать `Link`
   в `DropdownItem` (у него сейчас только `CommonAttrs`), отрендерить скрытый `<a>`, а в `crates/web`
   научить обработчик выбора кликать по нему с учётом модификаторов. Отдельная задача, не «обновление стилей».
9. **`Button` в состоянии loading** (#2694): upstream заменил `visibility: hidden` на `opacity: 0` +
   `pointer-events: none`, чтобы label оставался в дереве доступности, и добавил `aria-busy`.
   У нас в `button.css` нет ни `loading`, ни спиннера, ни каретки, ни позиционирования бейджа — фиксировать
   нечего. Учесть, когда `loading` будет реализован.
10. **Drawer: `<header>`/`<footer>` → `<div>`** (#2737). Не стилевой, но из той же версии: внутри `<dialog>`
    эти теги создают дублирующиеся landmark'и `banner`/`contentinfo`. У нас `drawer.rs:109` и `:181` рендерят
    ровно их, а `drawer.css` селекторы классовые (`.drawer-header`, `.drawer-footer`) — правка чисто в Rust,
    стили не задеваются вообще.

## 4. Спорные места — механическое обновление сломает наши наработки

**A. Кнопка очистки в `select.css` (#2748) — главный конфликт.**
Upstream сделал клик-таргет во всю высоту контрола: `align-self: stretch; inline-size: 1.5em;
margin-inline-start: calc(var(--wa-form-control-padding-inline) - 0.125em); margin-inline-end: -0.125em`.
У нас там намеренно `width: 1em; height: 1em` с комментарием — это локальный фикс «прыжка» высоты комбобокса,
который возникает потому, что в light DOM служебная кнопка ловит `height: var(--wa-form-control-height)`
из региона Buttons в `native.css` (ровно то, что описано в `issues/isolate internal buttons.md`).
Плюс наш `.combobox` — это `min-height` + переносимые теги в режиме `multiple`: `align-self: stretch`
растянет кнопку на всю многострочную высоту, чего в upstream не бывает.
**Порядок работ:** сначала `issues/isolate internal buttons.md`, потом перенос геометрии; наоборот — вернём старые баги.

**B. `input.css`: `:focus-within` → `:has(input:focus, textarea:focus)` (#2750).**
Смысл апстримного изменения — не окольцовывать поле, когда фокус на внутренней кнопке (clear/password-toggle).
У нас этих кнопок нет: `Input` рендерит label + `.text-field` (только `<input class="control">`) + hint + children,
внутри поля фокусироваться нечему, и оба селектора сейчас эквивалентны. Менять можно (дёшево и не ломает),
но по-настоящему это понадобится вместе с реализацией кнопок. Отмечу: наш базовый прозрачный outline с
комментарием про «вспышку тёмного кольца» уже совпадает по смыслу с апстримным #2625 — этот фикс мы взяли раньше.

**C. `common/component/variants.css` и `size.css` разошлись принципиально.**
У нас `.neutral/.brand/.success/.warning/.danger` вместо `.wa-*`, плюс мёртвые в light DOM `:host([data-variant=…])`;
в `size.css` — свои `.size-extra-small … .size-extra-large` с `!important` рядом с апстримными `.wa-size-*`.
Любой механический ресинк из `*.styles.ts` это снесёт. Новый `.wa-invert` из 3.12 сюда добавлять смысла нет
(нет инвертирующего компонента) — только в `utilities/variants.css`, п.2.
Заодно риск, не связанный с 3.12: имена `.neutral`, `.brand`, `.danger` слишком общие для глобальной таблицы стилей.

**D. Намеренные расхождения, которые обязаны пережить любой ресинк:**
`layers.css` и `themes/default.css` — `.page` вместо `wa-page`; `utilities/prose.css` — `.callout` вместо `wa-callout`.
Это и есть три из четырёх отличий `common/` от upstream (четвёртое — п.3, наш отставший `th`).
Ставлю пометку, потому что diff-мёрдж «в лоб» затрёт их молча.

**E. `layers.css` — два мёртвых правила, вскрылись при сверке.**
- `.page :is(*, *:after, *:before) { scroll-margin-top: var(--scroll-margin-top) }` — переменной
  `--scroll-margin-top` в проекте нет (в `page.css` она называется `--page-scroll-margin-top`, и там же
  дублируется само правило). Объявление невалидно на этапе вычисления значения.
- `.page[view='mobile'] .wa-desktop-only` / `.page[view='desktop'] .wa-mobile-only` не срабатывают никогда:
  атрибут `view` на `.page` нигде не выставляется — в оригинале его пишет JS-компонент `wa-page` по ширине.
  При этом класс `wa-desktop-only` уже используется: `examples/client/src/lib.rs:142`.
  Лечится либо выставлением `view` из `crates/web` (resize observer, как в `wa-page`), либо переписыванием
  на медиазапросы. Заодно убрать `\`` в комментарии — остаток TS-шаблона.

**F. `common/component/form-control.css` и `common/component/visually-hidden.css` — мёртвый код.**
Ни один `index.css` их не импортирует, а внутри — `::part(...)`, невозможные в light DOM
(живой аналог `.wa-visually-hidden` лежит в `utilities/`). Апстримный фикс `.has-count` в `form-control`
(#2720, для счётчика символов textarea) переносить в мёртвый файл смысла нет. Решение — либо перевести
на классы и подключить, либо удалить.

**G. `button.css`: иконочные кнопки.**
У нас `.button:has(.icon) { padding: 0 0.75em }`, у upstream — `.is-icon-button { width: var(--wa-form-control-height);
aspect-ratio: 1; outline-offset: 2px }` (квадратная кнопка) плюс отдельная ветка для каретки.
В 3.12 не менялось, трогать не нужно, но при следующем ресинке кнопки это первое место конфликта.

## 5. Что из 3.12 не касается проекта

`otp-input`, `pagination`, `toast`/`toast-item`, `data-grid`, `color-picker`, `textarea`, `popover`,
а также `styles/component/segmented-field.styles.ts` и `host.styles.ts` — соответствующих компонентов в библиотеке нет.

Поведенческие изменения 3.12, не относящиеся к стилям, но которые стоит завести отдельно в `BACKLOG.md`:
light dismiss и отмена `wa-hide` у Tooltip (#2721), сдвиг подменю при нехватке места по краям и
`aria-posinset`/`aria-setsize` у Dropdown (#2738, #2708).

## Порядок внедрения

1. Пункты 1–5 и 7 (в п.7 — только скоупинг `svg` → `.icon svg`): чистый CSS, ничего не ломают.
2. Пункт 10 (drawer `<header>`/`<footer>` → `<div>`): одна правка в Rust, тесты рендера в `tests/drawer.rs` придётся обновить.
3. Пункт E: починить или снять мёртвые правила `layers.css`.
4. Пункт A — только после `issues/isolate internal buttons.md`.
5. Пункты 8, 9, B, C, F, G — отдельными задачами.

---

Выполни шаг 1, как указано в файле задачи (пункты 1-5 и 7).

---

● Шаг 1 выполнен: пункты 1–5 и 7. Пункт 6 подтверждён как «делать нечего».

## Что изменено

- `common/utilities/visually-hidden.css` — `top/left: 0 !important` (#2765). Файл синхронизирован с upstream дословно.
- `common/utilities/variants.css` — `.wa-invert` добавлен в селектор (#2678). Синхронизирован дословно.
- `common/native.css` — `font-size` у `th` перенесён внутрь `&:not([scope='row']):not(:has(+ td))` (#2729).
  Файл отличался от upstream только этим блоком, поэтому взят целиком; итоговый diff — ровно один целевой ханк.
- `components/switch.css` — фокус-ринг с `~ .track .thumb` на `~ .track` (#2749).
- `components/tooltip.css` — стрелка через `box-shadow: inset …` вместо `border-right`/`border-bottom` (#2638).
- `utils/icon.css` — глобальное правило `svg { … }` заскоплено.

Rust не тронут: `cargo test` — 96 тестов проходят.

## Поправка к анализу: `.icon svg` оказалось недостаточно

В анализе (п.7) было сказано «должно быть `.icon svg`». При проверке выяснилось, что этого мало.
`iconic` отдаёт голый `<svg>` без своего класса или data-атрибута (`iconic/src/hypertext.rs:14`),
а библиотека рендерит иконку напрямую, без обёртки `.icon`, в шести местах:
`tag.rs:68` (`.tag-remove`), `select.rs:153/157/230` (`.clear-button`, `.expand-icon`, `.check`),
`dropdown.rs:296/413` (`.check`, `.submenu-icon`), плюс два слота, куда иконку кладёт вызывающий код:
`.callout-icon` (`callout.rs:57`) и `.dropdown-item-icon` (`dropdown.rs:325`).

Из них собственные правила `svg` были только у `select.css` и `tag.css`; `.callout-icon`,
`.dropdown-item-icon`, `.check` и `.submenu-icon` у Dropdown остались бы без размеров.
Поэтому правило названо списком контейнеров, а не `.icon svg`:

```css
:is(.icon, .callout-icon, .dropdown-item-icon, .check, .submenu-icon, .expand-icon, .clear-button, .tag-remove) svg
```

Специфичность при этом не ломает существующие переопределения: `:is()` даёт (0,1,1), а
`.select .clear-button svg` и `.tag .tag-remove svg` — (0,2,1), то есть по-прежнему выигрывают.

`.dropdown-item-icon` в список попал не сразу — его нашла проверка (см. ниже), а не чтение кода.
Это аргумент за то, чтобы в будущем всё-таки вешать `.icon` на служебные иконки в самой библиотеке
(тогда список схлопнется до одного селектора), но это уже правка разметки и снапшот-тестов — отдельной задачей.

## Как проверялось

Headless Chromium через CDP, сборка `cargo make client` + `example-server` на :9080.

1. **Аудит всех `<svg>`**: обход 15 страниц галереи, 987 элементов. Ни одного с неправдоподобным
   размером (все нулевые — это скрытые состояния `CopyButton` и закрытые попапы). Аудит же и показал
   единственный непокрытый контейнер — `.dropdown-item-icon`, после чего он был добавлен в список.
2. **Скрытые слоты иконок** (закрытые меню и листбоксы принудительно раскрыты):
   `.dropdown-item-icon svg` → 14px, `.check svg` в Dropdown → 12.3×14, `.submenu-icon svg` → 7×14,
   `.option .check svg` в Select → 11/12/14/18px по размерным вариантам, `.callout-icon svg` → 15…25px,
   `.tag-remove svg` → 14/16/20px, `.expand-icon svg` → 12/14/16/20px. Везде высота = 1em своего контекста.
3. **Switch**: фокус на `.control` → `outline: solid 3px` на `.track`, на `.thumb` — `outline-style: none`.
   Скриншот подтверждает: кольцо обводит весь тумблер.
4. **Tooltip**: тултип открыт (фокусом на анкоре), стрелка 14×14,
   `box-shadow: rgb(27,29,38) -1px -1px 0px 0px inset`, `border-right-width: 0px`. Скриншот — стрелка на месте.
5. **`.wa-visually-hidden`**: `top: 0px`, `left: 0px`, `position: absolute`.
6. **`.wa-invert`**: на элементе с этим классом `--wa-color-fill-loud`/`on-loud`/`border-loud` совпадают
   с его же инвертированной нейтральной шкалой, а нативная `<button>` внутри `.wa-invert` красится
   `#e4e5e9`/`#101219` против `#2f323f`/белого снаружи — ровно то, что чинит #2678.
7. **`th`**: во вставленной таблице колоночный заголовок 14px, `th[scope=row]` 16px = ячейка 16px.

Браузер и сервер остановлены.

---

Выполни шаг 2.

---

● Шаг 2 выполнен: Drawer — `<header>`/`<footer>` → `<div>` (#2737).

## Что изменено

- `crates/lib/src/layout/drawer.rs` — `DrawerHeader` рендерит `<div>` вместо `<header>`,
  `DrawerFooter` — `<div>` вместо `<footer>`. Обоим структурам добавлен doc-комментарий с причиной,
  чтобы правку не «починили» обратно.
- `crates/lib/src/tests/drawer.rs` — обновлены ожидаемые строки в двух местах:
  хелпер `header()` и тест `footer_and_header_actions`.

Стили не тронуты: `drawer.css` селекторы классовые (`.drawer-header`, `.drawer-footer`),
на теги не завязан ни один селектор. В `crates/web` тоже нет обращений к этим тегам
(`layout/page.rs` ищет `.page > header`, но это хедер страницы, а не ящика).

`cargo +nightly fmt --check`, `cargo clippy --all-targets -D warnings`, `cargo test` (96 тестов) — чисто.

## Как проверялось

Headless Chromium + CDP, страница `/drawer`, дерево доступности через `Accessibility.getFullAXTree`.

1. **Разметка**: все 11 `.drawer-header` и 12 `.drawer-footer` на странице — `DIV`;
   `document.querySelectorAll('dialog header, dialog footer').length === 0`.
2. **Лендмарки при закрытом ящике**: ровно по одному `banner`, `main`, `complementary`, `contentinfo` —
   собственные лендмарки страницы, дублей нет.
3. **Лендмарки при открытом ящике**: пусто. Модальный `<dialog>` делает остальную страницу inert,
   и сам ящик больше ничего не добавляет.
4. **Контрфактическая проверка** — главное доказательство. В уже открытом ящике `.drawer-header`
   и `.drawer-footer` были заменены обратно на `<header>`/`<footer>`, и дерево доступности сразу
   показало `{banner: 1, contentinfo: 1}`. То есть до правки скринридер объявлял шапку и подвал ящика
   как banner и contentinfo всей страницы (собственные лендмарки страницы в этот момент inert,
   так что подменялись именно они).
5. **Поведение**: ящик открывается по `data-drawer="open"`, закрывается кнопкой в шапке
   (`dialog[open]` → 0). Скриншот открытого ящика с футером — вёрстка не изменилась.

Браузер и сервер остановлены.

---

При открытии Drawer в Chromium контент страницы на заднем фоне немного прыгает вправо (исчезает скролбар), а при закрытии Drawer контент снова прыжком возвращается назад. В Firefox такого нет.

---

● Починен прыжок контента при открытии Drawer в Chromium.

## Причина

`update_body_scroll_lock` в `crates/web/src/layout/drawer.rs` вешал блокировку скролла напрямую:
`body.style.overflow = "hidden"`. Скролбар при этом исчезает, ширина контента вырастает на его толщину,
и страница дёргается вправо; при закрытии — обратно. В Firefox на этой машине оверлейные скролбары,
поэтому там ничего не заметно.

При этом в проекте уже лежала правильная утилита — `common/utilities/scroll-lock.css`, синхронная с
upstream дословно. Она резервирует место через `scrollbar-gutter` и ждёт, что кто-то повесит на `<html>`
класс `.wa-scroll-lock` и выставит `--wa-scroll-lock-gutter`/`--wa-scroll-lock-size`. Драйвера у неё не было:
при портировании поведения ящика в Rust блокировку сделали «в лоб», и утилита осталась мёртвой.

## Что изменено

- Новый `crates/web/src/util/scroll.rs` — порт `internal/scroll.ts` из Web Awesome: меряет ширину
  скролбара (`innerWidth - documentElement.clientWidth` плюс уже имеющийся `padding-right` у `<body>`),
  выставляет `--wa-scroll-lock-gutter`/`--wa-scroll-lock-size` и вешает `.wa-scroll-lock` на `<html>`.
  Отличия от оригинала осознанные:
  - вместо `Set` запирающих элементов — булев параметр `set_body_scroll_lock(locked)`; состояние
    по-прежнему выводится из DOM (`.drawer.open`), как и было, и как принято в `crates/web`;
  - при разблокировке снимается не только `--wa-scroll-lock-size`, но и `--wa-scroll-lock-gutter`
    (upstream оставляет его висеть — безвредно, но грязно).
  Сохранены обе тонкости оригинала: мерить строго **до** навешивания класса (после `overflow: hidden`
  скролбара уже нет и померится ноль) и не трогать gutter, если ширина меньше 2px (это округление,
  а не скролбар).
- `crates/web/src/layout/drawer.rs` — `update_body_scroll_lock` теперь зовёт `scroll::set_body_scroll_lock`.

`cargo make lint-test` — чисто (96 тестов).

## Как проверялось

Headless Chromium (реальный классический скролбар шириной 15px), страница `/drawer`.
Замерялся правый край правого элемента шапки и правый край колонки контента.

| | сдвиг при открытии | сдвиг при закрытии |
|---|---|---|
| до правки (контрфактически: инлайновый `overflow: hidden`) | шапка **+15.00px**, контент **+7.50px** | −15.00 / −7.50 |
| после правки | **0.00px** | **0.00px** |

`documentElement.clientWidth` по-прежнему меняется на 15px — скролбар действительно исчезает, — но
`scrollbar-gutter: stable` держит место, и ничего не двигается.

Дополнительно:
- состояние при открытом ящике: `.wa-scroll-lock` на `<html>`, `--wa-scroll-lock-gutter: stable`,
  `--wa-scroll-lock-size: 15px`, вычисленный `scrollbar-gutter: stable`, `body { overflow: hidden }`;
  после закрытия — всё снято;
- закрытие кнопкой в шапке и по [Escape], два полных цикла открыть/закрыть подряд — ни остаточного
  класса, ни остаточных переменных, ни горизонтального сдвига;
- при прокрутке вглубь страницы (scrollY ≈ 2800) сдвига тоже нет.

## Замечено попутно, не чинил

- При открытии ящика вертикальная прокрутка сдвигается примерно на 50px: `show()` вызывает
  `html.focus()` на `<dialog>`, и браузер подтягивает его в видимую область. Проверено, что к блокировке
  скролла это отношения не имеет — сдвиг воспроизводится при полностью нейтрализованной
  `scroll-lock.css`. Обратно при закрытии не возвращается.
- Пока ящик открыт, справа видна пустая полоса шириной со скролбар — это и есть зарезервированный
  gutter, и `dialog::backdrop` её не закрывает. Ровно так же ведёт себя Web Awesome: это цена отказа
  от прыжка контента.

---

Теперь появляется белая полоса на месте скроллбара, тогда как основной контент сереет во время открытия Drawer.

---

● Убрана белая полоса на месте скролбара при открытом Drawer.

## Причина

Замер по пикселям: затемнение обрывается ровно на правом крае `body` (x=1425), а последние 15px —
чистый белый. `scrollbar-gutter: stable` резервирует место **вне** content-box корневого элемента,
и `::backdrop` туда не достаёт: он покрывает ICB, то есть ту же область, что и `body`. В полосе
проступает фон `<html>` (белый), не затемнённый.

То есть предыдущая правка убрала прыжок, но заплатила за это незатемнённой полосой — как я и отметил
в прошлом отчёте. Оказалось, платить не обязательно.

## Что изменено

Взят второй способ компенсации, который upstream и так везёт — тот, что у него стоит в ветке
`@supports not (scrollbar-gutter: stable)` для старых Safari: не резервировать gutter на `<html>`,
а добавить паддинг `body`. Контент встаёт ровно туда же, корневой элемент сохраняет полную ширину,
и `::backdrop` накрывает всё.

- Новый `webassets/style/utils/scroll_lock.css` — `.wa-scroll-lock body { padding-right: var(--wa-scroll-lock-size) }`,
  подключён в `index.css`.
- `crates/web/src/util/scroll.rs` — убрано вычисление и выставление `--wa-scroll-lock-gutter`
  (вместе с функцией `gutter()`); остаётся только `--wa-scroll-lock-size` и класс.

Ключевая деталь, почему сделано именно «не выставлять переменную», а не «переопределить»:
апстримное правило — `scrollbar-gutter: var(--wa-scroll-lock-gutter) !important` внутри
`@layer wa-utilities`, а `!important` в слое бьёт `!important` без слоя (для важных объявлений
порядок слоёв инвертируется). Переопределить его из своего слоя нельзя. Зато если переменную
не задавать вовсе, `var()` невалиден на этапе вычисления значения и свойство схлопывается в `auto` —
ветка становится инертной сама собой. Это зафиксировано комментариями в обоих файлах.

`common/utilities/scroll-lock.css` при этом остался дословной копией upstream — расхождений в
синхронизируемом каталоге не добавилось, и следующий ресинк ничего не затрёт.

## Как проверялось

Chromium, реальный скролбар 15px, страница `/drawer`.

- **Полосы нет**: строка пикселей y=400 от x=1400 до правого края (1439) — один сплошной
  `rgb(135,136,140)`. До правки на x=1425 начинался `rgb(255,255,255)`.
- **Сдвига по-прежнему нет**: правые края шапки (1425), контента (1079), меню (240) и кнопки
  GitHub (1413) одинаковы в закрытом и открытом состоянии — сдвиг 0.00px по всем четырём.
- **Состояние при открытом ящике**: `body { padding-right: 15px }`, `--wa-scroll-lock-size: 15px`,
  `--wa-scroll-lock-gutter` не задан, вычисленный `scrollbar-gutter: auto`.
- **Уборка**: закрытие кнопкой и по [Escape], два полных цикла подряд — паддинг снимается,
  класс и переменная снимаются, край не уезжает.

`cargo make lint-test` — чисто (96 тестов). Браузер и сервер остановлены.

---

Далее следует выполнять шаг 3. Но там вопрос: как лучше выставлять view?


---

● Шаг 3 выполнен: мёртвые правила `layers.css` ожили. `view` выставляется `ResizeObserver`'ом,
у `Page` появилось мобильное боковое меню.

## Как решён вопрос с `view`

Взята механика `wa-page`: `ResizeObserver` на самом элементе `.page`, сравнение его `border-box`
inline-размера с брейкпоинтом. Наблюдается именно элемент, а не вьюпорт, — тогда узко свёрстанная
страница внутри широкого окна тоже схлопнется.

Отличия от upstream, сознательные:

- **Брейкпоинт по умолчанию 920px, а не 768px.** В `page.css` уже был viewport-медиазапрос на 920px;
  оставить апстримные 768px значило получить на одной странице два расходящихся брейкпоинта.
  Переопределяется атрибутом `data-mobile-breakpoint` (`px`, `rem`, `em`).
- **`view` — единственный источник истины.** Адаптивная раскладка в `page.css` переведена
  с `@media` на `.page[view='mobile']` / `[view='desktop']`. Upstream держит и то и другое,
  синхронизируя их инжектом стилей на экземпляр; нам это не нужно, зато брейкпоинт остаётся один
  и настраиваемый.
- **`view="desktop"` рендерится сервером.** Ровно по причине, которую upstream указывает в доке:
  мобильная навигация требует ящика, ящик требует JS, поэтому до загрузки WASM показываем ту
  раскладку, которая работает без него.

## Мобильное меню

`Page` получил проп `navigation`: при нём рендерится `Drawer` с `id="page-navigation"`.
Новый компонент `PageNavigationToggle` — гамбургер с `data-toggle-nav` и `data-drawer="open page-navigation"`.
Второй атрибут означает, что открытие идёт через уже существующий декларативный механизм ящиков —
никакого нового обработчика кликов в `crates/web` не понадобилось.

Контент меню при смене `view` **физически переносится** между `.page-menu` и телом ящика. Это
светло-DOM'ный аналог переназначения слотов, которым upstream пользуется в shadow root: навигация
существует в одном экземпляре, поэтому её переносят, а не дублируют — иначе разъехались бы `id`
и обработчики. Проверено, что htmx-привязки перенос переживают.

## Изменённые файлы

- `crates/lib/src/layout/page.rs` — `navigation` и `mobile_breakpoint` у `Page`, рендер `view`
  и ящика, новый `PageNavigationToggle`.
- `crates/lib/src/class.rs` — `PAGE_NAVIGATION_DRAWER`, `PAGE_NAVIGATION_TOGGLE`.
- `crates/web/src/layout/page.rs` — `ResizeObserver`, разбор брейкпоинта, перенос навигации,
  закрытие ящика при возврате на десктоп.
- `crates/web/Cargo.toml` — фичи `ResizeObserver*`.
- `webassets/style/layouts/page.css` — раскладка на `[view]`, стили гамбургера и ящика,
  `--page-scroll-margin-top` переименован в `--scroll-margin-top`, наш дублирующий
  `scroll-margin-top` удалён.
- `examples/client/src/lib.rs` — `<Page navigation=true>` и `<PageNavigationToggle/>` в шапке.
- `crates/lib/src/tests/page.rs` — обновлён `empty`, добавлен `navigation`.

`layers.css` не тронут: все три его правила теперь имеют потребителя.

### Одна тонкость каскада

Гамбургер прячется собственным правилом `page.css`, а не апстримным
`.page[view='desktop'] [data-toggle-nav] { display: none }`. Причина: апстримное правило лежит
в `@layer wa-base`, а `page.css` не в слое, и безусловный `display: inline-flex` его перебивал бы
(неслоёные обычные объявления сильнее слоёных). Поэтому видимость гамбургера тоже привязана
к `view`. Апстримное правило при этом продолжает работать для кастомных `[data-toggle-nav]`,
которые может добавить приложение.

## Как проверялось

Headless Chromium, изменение ширины через `Emulation.setDeviceMetricsOverride`.

| ширина | view | детей в `.page-menu` | детей в ящике | гамбургер | `#brand-name` (`wa-desktop-only`) |
|---|---|---|---|---|---|
| 1440 | desktop | 6 | 0 | `none` | `block` |
| 700 | mobile | 0 | 6 | `flex` | `none` |
| 1440 (обратно) | desktop | 6 | 0 | `none` | `block` |

- Клик по гамбургеру открывает ящик, блокировка скролла срабатывает; [Escape] закрывает.
- Ссылка **внутри ящика** переключает раздел: контент сменился с Badge на Switch. То есть htmx
  пережил перенос узлов. (Сначала я решил, что связь потеряна, — ошибся: смотрел на
  `location.pathname`, который в этой галерее не меняется и при клике по обычному меню.)
- `data-mobile-breakpoint="40rem"` при ширине 701px даёт `desktop` — `rem` разрешается верно.
- Возврат на десктоп при открытом ящике закрывает его и возвращает меню на место.
- `--scroll-margin-top` теперь определён: `calc(63px + 0.5em)`, вычисленный `scroll-margin-top`
  на элементах внутри `.page` — `83.5px`. Правило из `layers.css` ожило, наш дубль удалён.
- Ошибок `ResizeObserver loop` в консоли нет.

`cargo make lint-test` — чисто, 97 тестов. Браузер и сервер остановлены.

## Не трогал

В комментарии `layers.css` остался экранированный обратный апостроф (`\``) — след TS-шаблона.
Он внутри комментария, ни на что не влияет, а правка добавила бы расхождение с upstream на пустом месте.

---

Ты написал: "Upstream держит и то и другое, синхронизируя их инжектом стилей на экземпляр; нам это не нужно" - но почему нам это не нужно?

---

● Проверил — утверждение было неверным. Нам это нужно. Я обобщил с галереи на библиотеку.

  Почему я ошибся

  Галерея — это WASM-SPA: #root пуст, пока не загрузится модуль, поэтому «до-JS» окна там просто нет. Но wingy-hypertext — серверно-рендерящая библиотека, и у её потребителя разметка приезжает готовой. Между
  приходом HTML и стартом WASM view="desktop" — это ложь, если экран узкий.

  Что нашлось в upstream

  Решающая деталь, которую я в прошлый раз пролистнул, — <style> рендерится внутри шаблона компонента, с явным комментарием:

  <!-- unsafeHTML needed for SSR until this is solved: lit/lit#4696 -->
  ${unsafeHTML(`<style id="mobile-styles">${mobileStyles(toLength(this.mobileBreakpoint))}</style>`)}

  То есть медиазапрос попадает в серверный вывод намеренно. И гамбургер там показывается дважды — через [view='mobile'] (page.styles.ts:255) и через медиазапрос. Это осознанная избыточность: атрибут точен после
  гидратации, медиазапрос работает до неё.

  Что ломается у нас

  Собрал статическую страницу из нашей же SSR-разметки без скриптов, открыл на 390px:
  
  ┌─────────────────────────────┬──────────────────────────┐
  │                             │         значение         │
  ├─────────────────────────────┼──────────────────────────┤
  │ view                        │ desktop (как в разметке) │
  ├─────────────────────────────┼──────────────────────────┤
  │ .page-menu                  │ display: block, 187.5px  │
  ├─────────────────────────────┼──────────────────────────┤
  │ .main-content               │ 733px                    │
  ├─────────────────────────────┼──────────────────────────┤
  │ aside                       │ 187.5px                  │
  ├─────────────────────────────┼──────────────────────────┤
  │ гамбургер                   │ display: none            │
  ├─────────────────────────────┼──────────────────────────┤                               
  │ documentElement.scrollWidth │ 921px при вьюпорте 390   │                               
  └─────────────────────────────┴──────────────────────────┘                               
  
  На скриншоте — трёхколоночная десктопная раскладка на телефоне с горизонтальной прокруткой. Ровно то, что медиазапрос в upstream и предотвращает.
  
  Варианты

  A. Как в upstream — Page рендерит <style> с медиазапросом на своём брейкпоинте. Точно, работает и с кастомным data-mobile-breakpoint до гидратации. Минус: генерируемый CSS в разметке, тогда как в репозитории
  CSS живёт в webassets/style.
  
  B. Фолбэк в page.css на дефолтных 920px — блок @media (width < 920px) { .page[data-view-pending] { … } }, где data-view-pending ставит Page и снимает клиент при первом замере. Правило само отключается после
  гидратации. Минус: до гидратации фолбэк всегда 920px, кастомный брейкпоинт вступает в силу только после.
  
  C. B по умолчанию + A только когда задан кастомный брейкпоинт.
  
  Рекомендую B: CSS остаётся там, где ему положено, дефолтный брейкпоинт покрывает подавляющую часть случаев, а после гидратации поведение и так точное.
  
  Отдельный вопрос внутри любого варианта: показывать ли гамбургер до гидратации. Upstream показывает, но его ящик тоже требует JS — то есть кнопка мертва. Я бы до гидратации только схлопывал сетку в одну
  колонку и прятал меню, а гамбургер не показывал. Это расхождение с upstream, поэтому спрашиваю, а не решаю сам.
  
  Какой вариант берём?

---

Берём А. Гамбургер показываем, как в upstream.

---

● Реализую A. Сначала найду, как в hypertext вывести сырой текст в <style>
● Разметка верная. Обновляю тесты и добавляю проверку санитизации брейкпоинта.
● Теперь проверю то, ради чего всё делалось: SSR-страница без JS на 390px.

---

● Добавлен пре-гидратационный фолбэк (вариант A, как в upstream).

## Зачем

Проверка показала, что раньше сказанное мной «нам это не нужно» было неверным. Галерея — WASM-SPA
с пустым `#root` до загрузки модуля, поэтому до-JS окна там нет; но `wingy-hypertext` — серверно
рендерящая библиотека, и у её потребителя разметка приезжает готовой. До старта WASM `view="desktop"`
на узком экране — ложь.

Замер статической страницы, собранной из нашей же SSR-разметки без скриптов, вьюпорт 390px:

| | было | стало |
|---|---|---|
| `.page-menu` | `block`, 187.5px | `none`, 0 |
| `aside` | 187.5px | 0 |
| гамбургер | `none` | `flex` |
| `documentElement.scrollWidth` | **921px** | 733px |

733px — это уже переполнение содержимым (код-примеры), а не оболочкой: у гидратированной страницы
на той же ширине ровно те же `mainW: 733` и `scrollWidth: 733`. То есть без-JS страница теперь
совпадает с гидратированной, что и требовалось.

## Что сделано

`Page` рендерит `<style>` с медиазапросом на своём брейкпоинте — ровно как `wa-page`, у которого
этот блок лежит прямо в шаблоне с комментарием «unsafeHTML needed for SSR». Держать это в `page.css`
нельзя: брейкпоинт задаётся на экземпляр.

Содержимое зависит от `navigation`:

- с навигацией — схлопывание сетки, `.page-menu { display: none }`, `.page-navigation-toggle { display: inline-flex }`;
- без неё — только `--menu-width: auto; --aside-width: auto`, то есть в точности то, что делал
  прежний `@media (max-width: 920px)` до перехода на `view`.

Гамбургер до гидратации показывается — как и договаривались, по образцу upstream (у него кнопка
до загрузки JS тоже неактивна, ящик требует скриптов в обоих случаях).

### Санитизация

Брейкпоинт приходит от вызывающего кода и попадает в прелюдию `@media` дословно, поэтому
`safe_breakpoint` пропускает только CSS-длину: строка обязана начинаться с цифры или точки и
состоять из цифр, точки, латинских букв и `%`. Всё остальное заменяется на `920px`.
Санитизированное значение идёт и в атрибут `data-mobile-breakpoint` — его разбирает клиент, и
расходиться с медиазапросом он не должен.

Тест `breakpoint_injection_is_refused` проверяет обе стороны: на входе
`1px){} body{display:none}@media(width>0px` в `<style>` остаётся дефолтный медиазапрос,
а в атрибуте — `920px`. Заодно проверено, что легитимное `48.5em` проходит.

## Изменённые файлы

- `crates/lib/src/layout/page.rs` — `DEFAULT_MOBILE_BREAKPOINT`, `safe_breakpoint`,
  `mobile_stylesheet`, рендер `<style>` и санитизированного атрибута.
- `crates/lib/src/tests/page.rs` — обновлены `empty` и `navigation`, добавлен
  `breakpoint_injection_is_refused`.

`cargo make lint-test` — чисто, 98 тестов.

## Известное ограничение

`.wa-desktop-only` / `.wa-mobile-only` до гидратации не срабатывают: они привязаны к `view`
в `layers.css`, а в инжектируемый блок upstream их не кладёт (там только навигация и переключатель).
Поэтому на скриншоте без-JS страницы название бренда ещё видно. Это ровно поведение upstream;
если захочется, эти два класса можно добавить в `mobile_stylesheet` — но это уже расхождение.

---

Что даст добавление этих двух классов в mobile_stylesheet?

---

● Проверил в браузере на изолированном тесте — ответ получился асимметричным.

  Что даст: только половину
  
  Правило в layers.css одно, но покрывает два класса — и они ведут себя по-разному:
  
  @layer wa-base {
    .page[view='mobile'] .wa-desktop-only,
    .page[view='desktop'] .wa-mobile-only { display: none !important; }
  } 
  
  До гидратации view="desktop", поэтому на узком экране .wa-desktop-only показан (должен быть скрыт), а .wa-mobile-only скрыт (должен быть показан).
  
  ┌──────────────────┬───────────────────┬─────────────┐
  │                  │ что нужно сделать │ получается? │
  ├──────────────────┼───────────────────┼─────────────┤
  │ .wa-desktop-only │ скрыть            │ ✅ да       │
  ├──────────────────┼───────────────────┼─────────────┤
  │ .wa-mobile-only  │ показать          │ ❌ нет      │
  └──────────────────┴───────────────────┴─────────────┘
  
  Замер (view="desktop", вьюпорт 390px, инжектируемый блок вне слоёв):
  
  desktopOnly:       none     ← сработало
  mobileOnly_revert: none     ← display: revert !important — проиграл
  mobileOnly_hiSpec: none     ← даже (0,4,0) с !important против их (0,3,0) — проиграл
  
  Скрыть получается, потому что скрывающему правилу никто не мешает: слоёное правило до гидратации не матчится, а обычного объявления вне слоёв достаточно. Показать не получается: display: none !important лежит
  внутри @layer wa-base, а для важных объявлений порядок слоёв инвертируется — важное вне слоёв слабее важного в слое. Специфичность тут не помогает вовсе: происхождение и важность решаются раньше неё. Это та
  же особенность каскада, из-за которой гамбургер пришлось прятать своим правилом, а не апстримным.
  
  Насколько это вообще ценно сейчас
  
  - .wa-desktop-only — один потребитель во всём репозитории: название бренда в шапке галереи (examples/client/src/lib.rs:143).
  - .wa-mobile-only — ноль потребителей.
  
  То есть практический выигрыш сегодня: один <span> перестанет мелькать на узком экране до загрузки WASM у SSR-потребителя.
  
  Оценка                                                                                   
                                                                                           
  Я бы не добавлял. Аргумент не в цене (одно правило), а в том, что получится асимметрия: desktop-only чинится до гидратации, mobile-only — нет. Пара классов задумана как симметричная, и «работает наполовину»
  объяснить труднее, чем «оба класса начинают действовать после гидратации».

  Если симметрия нужна по-настоящему, чинить надо не mobile_stylesheet, а layers.css — убрать оттуда !important или перевести правило на что-то, что медиазапрос тоже может выставить. Но это расхождение в дословно синхронизируемом файле, то есть ровно то, чего мы весь этот заход избегали.

---

Если чинить как следует, не взирая на совместимость стилей, то как следует сделать?

---

● Хороший вопрос. Есть вариант, который убирает саму причину, а не симптом. Проверю его на реальной странице.

  Ran 3 shell commands 

● Если не оглядываться на совместимость с upstream — правильный ответ не «убрать !important», а выкинуть view из стилей и перейти на container queries.

  Почему именно так

  view существует ровно потому, что медиазапрос меряет вьюпорт, а нужна ширина элемента. Container query меряет именно элемент — то есть делает без единой строки JS ровно то, ради чего заведён ResizeObserver.
  Атрибут после этого перестаёт быть источником истины для CSS, и вся проблема пре-гидратации исчезает не «чинится», а перестаёт существовать.

  Проверил на изолированной странице без скриптов:

  ┌──────────────────────────────┬──────────────┬───────────┐
  │           сценарий           │ ширина .page │ результат │
  ├──────────────────────────────┼──────────────┼───────────┤
  │ вьюпорт 1440                 │ 1425         │ desktop   │ 
  ├──────────────────────────────┼──────────────┼───────────┤
  │ вьюпорт 700                  │ 685          │ mobile    │ 
  ├──────────────────────────────┼──────────────┼───────────┤
  │ вьюпорт 1440, страница 600px │ 600          │ mobile    │ 
  └──────────────────────────────┴──────────────┴───────────┘
  
  Третья строка — то, что медиазапрос принципиально не может: он сказал бы desktop. То есть container query не просто закрывает пре-гидратационное окно, он ещё и точнее апстримного фолбэка — совпадает с тем,
  что меряет ResizeObserver.
  
  Побочные эффекты container-type: inline-size (это contain: layout style inline-size) проверил отдельно, потому что layout-containment по спецификации делает элемент содержащим блоком для fixed:
  
  - position: fixed внутри .page после прокрутки на 300px остался на top: 10 — привязка к вьюпорту сохранилась;
  - модальный <dialog> занял 1419×794 при вьюпорте 1440×800 — top layer от containment не зависит, наш ящик не страдает.
  
  Это важно, потому что на position: fixed у нас держатся popup, а через него — Dropdown, Select и Tooltip. Проверено в Chromium; при реализации это первое, что нужно перепроверить в Firefox и Safari.
  
  Как это выглядит                                                                         
  
  1. page.css: на .page — container-type: inline-size; container-name: page. Дефолт (десктоп) остаётся там же, обычными правилами.
  2. Page рендерит тот же <style>, но с @container page (width < BP) вместо @media. Генерировать по-прежнему нужно: var() в условии контейнерного запроса не работает, а брейкпоинт задаётся на экземпляр.
  3. В этот блок переезжают все мобильные переопределения — включая три правила, которые сейчас в layers.css: .wa-desktop-only, .wa-mobile-only и [data-toggle-nav]. Из layers.css они удаляются. Каскадной драки
     больше нет: блок один и вне слоёв, !important не нужен нигде.
  4. crates/web сохраняет ResizeObserver, но уже только чтобы переносить навигацию и закрывать ящик — это DOM-операции, их CSS не сделает. view можно продолжать выставлять как информационный хук для прикладного
     кода, но стили от него больше не зависят.                                             
                                                                                           
  Чем платим

  - Расхождение в layers.css — три правила удаляются. Осознанное и документируемое, но в дословно синхронизируемом файле.
  - Baseline. Container queries: Chrome 105 / Safari 16.0 / Firefox 110. Проект уже требует color-mix() (Chrome 111 / Safari 16.2 / Firefox 113), то есть планку это не поднимает — она уже выше.
  - Генерируемый блок вырастет: сейчас там 3–4 объявления, станет ~10, потому что все мобильные переопределения переезжают в него.
  - Containment на .page надо проверить на всех трёх движках, а не только в Chromium. 
  
  Что это даёт в сумме                                                                     
  
  Один источник истины для стилей, работающий с первого кадра и без JS; пре-гидратационного окна нет вовсе — в том числе для обоих utility-классов; поведение точнее апстримного, потому что меряется элемент;
  !important и борьба со слоями уходят совсем.
  
  Браться? Объём — правки в page.css, layout/page.rs, layers.css и тестах; crates/web почти не меняется.

---

Да, берём в работу.

---

● Переход на container queries: `view` больше не участвует в стилях.

## Что изменилось по сути

Раньше «узкая ли страница» отвечали два механизма: атрибут `view` (точный, но только после
гидратации) и медиазапрос (без JS, но меряет вьюпорт, а не элемент). Их приходилось держать
согласованными, и один из них всегда был неправ в своём окне.

Container query отвечает на этот вопрос один раз и правильно: он меряет **элемент**, как
`ResizeObserver`, и работает **без JS**, с первого кадра. После этого `view` в стилях не нужен вовсе.

## Изменённые файлы

- `webassets/style/layouts/page.css` — на `.page` добавлены `container-type: inline-size` и
  `container-name: page`; правила `[view='desktop']` / `[view='mobile']` заменены обычными
  «широкими» умолчаниями, которые переопределяет генерируемый запрос.
- `crates/lib/src/layout/page.rs` — `mobile_stylesheet` генерирует `@container page (width < BP)`
  вместо `@media`; в него переехали `.wa-desktop-only`, `.wa-mobile-only` и `[data-toggle-nav]`.
- `webassets/style/common/layers.css` — три правила на `view` **удалены** (осознанное расхождение
  с upstream, на их месте комментарий с объяснением).
- `crates/web/src/layout/page.rs` — `ResizeObserver` остался, но теперь только переносит навигацию
  и закрывает ящик; `view` выставляется как информационный хук, стили от него не зависят.
- `crates/lib/src/tests/page.rs` — обновлены ожидаемые строки.

Каскадной драки больше нет нигде: генерируемый блок один и вне слоёв, `!important` в нём стоит
только там, где он был у upstream (утилиты видимости).

## Как проверялось

### Без JS вообще (статическая страница из нашей SSR-разметки, `view="desktop"` в разметке)

| | 390px | 1440px |
|---|---|---|
| `.page-menu` | `none` | `block` |
| гамбургер | `flex` | `none` |
| `#brand-name` (`.wa-desktop-only`) | **`none`** | `block` |

Третья строка — то, что предыдущим вариантом починить было нельзя в принципе. Пре-гидратационного
окна не осталось: без единого скрипта раскладка верна на обеих ширинах.

### С JS

1440 → 700 → 1440: `view` переключается, меню переезжает в ящик и обратно (6 детей туда, 6 обратно),
гамбургер и бренд появляются и исчезают, ящик открывается по клику и закрывается по [Escape].

### Containment — главный риск

`container-type: inline-size` — это `contain: layout style inline-size`, а `layout`-containment по
спецификации делает элемент содержащим блоком для `position: fixed`. У нас на `fixed` держится
`popup`, а через него Dropdown, Select и Tooltip.

- Select, прокрутка `scrollY: 1500`: тело попапа `position: fixed`, зазор до комбобокса **0px**,
  попап внутри вьюпорта. Привязка не сломалась.
- Tooltip: привязан к анкеру.
- Модальный `<dialog>` (наш ящик) занимает весь вьюпорт — top layer от containment не зависит.

Проверено в Chromium. **В Firefox и Safari это стоит перепроверить** — поведение containment
относительно `fixed` там может отличаться, а тестового окружения для них здесь нет.

### Baseline

Container queries: Chrome 105 / Safari 16.0 / Firefox 110. Проект уже требует `color-mix()`
(Chrome 111 / Safari 16.2 / Firefox 113), так что планка не поднялась — она и так была выше.

`cargo make lint-test` — чисто, 98 тестов. Браузер и сервер остановлены.

---

С какого-то момента в example web-app сломалось указание выбранного раздела в location (url path). В чём причина?

---

● Проверю. Я это заметил по ходу работы, но отнёс к особенностям галереи — посмотрю как следует.
● Причина найдена, и это не мои правки.

  Когда сломалось

  Коммит fedf686 «feat: impl client-router htmx ext» (23 августа). Он заменил перехват XHR (client_patch.js) на htmx-расширение client-router.ext.js.

  Почему

  Расширение отменяет запрос на htmx:configRequest и само делает подстановку:

  event.preventDefault();
  let client_html = router(path, event);
  htmx.swap(target, client_html, { swapStyle: swap_spec });
  
  А htmx на отменённое configRequest выходит из issueAjaxRequest немедленно — вот это место в 2.0.8:
  
  if (!ae(r, "htmx:configRequest", C)) { re(s); m(); return e }
  
  Всё, что идёт дальше по конвейеру, не выполняется вовсе. В том числе обработка hx-push-url: она живёт в разборе ответа и читает t.pathInfo.finalRequestPath / responsePath, то есть данные, которых без запроса
  просто не существует. А htmx.swap() — низкоуровневый API: он подставляет содержимое и запускает settle, но историю не трогает по определению.
  
  Старый client_patch.js подделывал сам XHR, поэтому htmx проходил свой конвейер целиком и hx-push-url отрабатывал штатно.
  
  Замеры
  
  start          : path='/'  main=''                    historyLen=2
  after /badge   : path='/'  main='BadgeBadge<Badge>…'  historyLen=2                       
  after /switch  : path='/'  main='SwitchSwitches…'     historyLen=2                       

  Контент подставляется, URL не меняется, в историю ничего не пишется. Второе следствие, о котором стоит знать: history.back() уводит из приложения целиком — внутренних записей истории нет, так что «Назад»/«Вперёд» между разделами не работают.

  Проверка диагноза «в лоб»: навесил на htmx:configRequest свой обработчик, который делает только history.pushState(…, e.detail.path) — и всё встаёт на место:

  with manual push : path='/switch'  main='SwitchSwitches…'  historyLen=3

  Глубокие ссылки при этом целы: сервер отдаёт index.html на любой односегментный путь без точки, а main.js рендерит wasm.render_root(window.location.pathname). Прямой заход на /badge работает — сломано именно проставление URL при переходах.
                                                                                           
  Что нужно, чтобы починить

  Расширение должно взять на себя то, от чего отказалось вместе с запросом:
  
  1. hx-push-url / hx-replace-url → history.pushState / replaceState на e.detail.path;     
  2. обработчик popstate, чтобы «Назад»/«Вперёд» перерисовывали раздел через тот же router(path).
  
  Второй пункт htmx тоже брал на себя (у него собственный кеш истории), и без запроса он тоже отключился.
  
  Браться за реализацию?

---

Да, реализуй. Необходимо, чтобы расширение работало не хуже, чем прошлый патч: все функции htmx должны отрабатывать, как будто бы запрос пришёл с сервера, а не с клиентского роутера.

---

● Требование меняет подход: чинить надо не два места, а транспорт. Смотрю, где в htmx можно вклиниться.
● Понял: чинить надо на уровне транспорта, чтобы htmx прошёл весь конвейер. Точка входа — htmx:beforeSend.
● Проверю настоящий промах кеша истории — там есть риск, что фрагмент подставится не туда.
● Промах кеша ведёт себя не так, как я заложил, — проверил исходник htmx: восстановление подставляет ответ в body, а роутер отдаёт фрагмент. Убираю эту ветку.
● Починено. Причина — не мои правки, а коммит fedf686 от 23 августа.
                                                                                           
  Причина                                                                                  
                                                                                           
  Расширение отменяло запрос на htmx:configRequest и подставляло содержимое само. Но htmx на отменённое configRequest выходит из issueAjaxRequest немедленно:
                                                                                           
  if (!ae(r, "htmx:configRequest", C)) { re(s); m(); return e }                            
                                                                                           
  Всё дальше по конвейеру не выполняется. hx-push-url разбирается при обработке ответа и читает pathInfo.responsePath — данные, которых без запроса не существует. А htmx.swap() историю не трогает по
  определению. Вместе с историей отключились OOB-подстановки, hx-select, заголовки HX-*, индикаторы, afterRequest/afterOnLoad и обработка ошибок.
                                                                                           
  Решение                                                                                  
                                                                                           
  Перехват перенесён на уровень транспорта — htmx:beforeSend. К этому моменту htmx уже собрал запрос и повесил на XHR свой onload со всей обработкой ответа. Расширение глушит отправку у этого экземпляра
  (xhr.send = () => {}, прототип не трогается — то, из-за чего когда-то ушли от глобального патча), подменяет на нём status/response/responseURL/заголовки и вызывает xhr.onload() через setTimeout(0). Дальше
  htmx работает как обычно: для него ответ пришёл с сервера.                               
                                                                                           
  Проверка                                                                                 
                                                                                           
  URL и история:                                                                           
                                                                                           
  click /badge   path='/badge'   len=3      back    path='/switch'                         
  click /switch  path='/switch'  len=4      back    path='/badge'                          
  click /tooltip path='/tooltip' len=5      forward path='/switch'                         
                                                                                           
  Конвейер целиком: beforeRequest → beforeSend → beforeSwap → historyItemCreated → pushedIntoHistory → afterSwap → afterRequest → afterOnLoad → afterSettle. Наружу не уходит ничего — send не вызывается ни разу,
  htmx-request снимается.                                                                  
                                                                                           
  То, чего ручная подстановка не умела в принципе: OOB-swap (дописал в ответ hx-swap-oob — htmx нашёл и подставил в отдельный элемент), hx-select=".head" (в цель попали только семь совпавших элементов),
  hx-swap="outerHTML".                                                                     
                                                                                           
  Мобильный ящик не сломался: ссылка изнутри ведёт на /dropdown и обновляет URL.           
                                                                                           
  Одно решение, которое я принял по ходу                                                   
                                                                                           
  Написал было ветку для htmx:historyCacheMiss и убрал после чтения исходника: этот путь подставляет ответ в history-элемент, то есть в body, а роутер отдаёт фрагмент для .main-content — снесли бы всю оболочку.
  У старого патча был тот же изъян. Вместо этого в main.js выставлен htmx.config.refreshOnHistoryMiss = true: сервер отдаёт оболочку на любой односегментный путь, так что перезагрузка всегда даёт верный
  результат, а расширение остаётся общего назначения. Принудительный промах проверил — /tooltip восстанавливается корректно.
                                                                                           
  Оговорка: события прогресса (htmx:xhr:loadstart/progress/loadend) не возникают — для локального ответа их нечему порождать. Прежний патч их тоже не подавал, так что планка «не хуже» выдержана.
                                                                                           
  cargo make lint-test — чисто, 98 тестов. Описание завёл отдельным файлом issues/client-router push-url.md, а не дописал в issue про стили — задача не связана с 3.12.

---

Часто после активного перехода по компонентам и сокращении окна браузера и восстановлении на весь экран, при очередном сокращении появляется меню, но drawer его пустой. Вроде бы это происходит после того, как были использованы переходы по истории "назад".

Проверялось в Firefox

---

● Похоже на конкретный механизм. Сначала воспроизведу.
● Firefox и geckodriver есть — проверю в обоих. Сначала воспроизведение в Chromium.
● Воспроизвёл в Chromium — значит, механизм не зависит от движка. Смотрю код и соглашения по маркерам инициализации.
● Chromium подтверждает фикс. Теперь проверю в Firefox — там, где вы это видели.
● Проверю сам механизм в Firefox, а заодно закрою оставшийся с прошлой задачи вопрос про containment в этом движке.
● Ваша гипотеза верна — дело именно в переходах «назад». Механизм при этом от движка не зависит: воспроизвёл и в Chromium.

  Причина
  
  htmx восстанавливает запись истории, подставляя снимок в history-элемент, то есть в body. Элемент .page, за которым следил ResizeObserver, при этом выбрасывается — проверил прямо в Firefox: window.__before 
  === document.querySelector('.page') после «назад» даёт false.
  
  Наблюдатель умирает вместе со старым узлом. А init_page_element отказывался инициализировать новый, потому что маркер data-initialized — атрибут, то есть часть разметки, и он приезжал обратно в составе
  снимка, удостоверяя, что элемент уже настроен.
  
  Дальше всё сходится с симптомом: CSS работает без JS, поэтому container query исправно схлопывает раскладку и показывает гамбургер, а переносить навигацию некому — она остаётся в скрытом .page-menu, ящик
  пуст.
  
  after back           view='desktop' menuKids=6 drawerKids=0   
  shrunk again 700     view='desktop' menuKids=6 drawerKids=0   ← view не переключился
  
  Исправление
  
  1. Маркер инициализации перенесён с атрибута на свойство объекта элемента (wgPageInitialized). Свойства не сериализуются в innerHTML, поэтому восстановленный узел приходит без маркера, инициализируется заново
     и получает свой наблюдатель.
  2. Положение навигации сверяется на каждом срабатывании, а не только при смене view. Раньше apply_view выходил досрочно при совпадении атрибута — а снимок может принести навигацию не на той стороне
     брейкпоинта при уже «правильном» атрибуте. Перенос идемпотентен, лишних движений это не добавляет.
  
  Проверка
  
  Chromium и Firefox (через geckodriver), сценарий из вашего отчёта — результат одинаковый:
                                                                                           
  after back               view='desktop' menuKids=6 drawerKids=0                          
  shrunk again 700         view='mobile'  menuKids=0 drawerKids=6   ← было пусто           
  restored again           view='desktop' menuKids=6 drawerKids=0                          
                                                                                           
  Обратный случай (снимок снят в мобильном виде, восстановлен на десктопе) тоже сходится: навигация возвращается в меню.
                                                                                           
  Попутно закрыл вопрос из прошлой задачи

  Раз уж Firefox оказался доступен, проверил в нём container-type: inline-size — не ломает ли layout-containment позиционирование fixed, на котором держатся попапы. Не ломает: containerType: inline-size
  активен, попап Select — position: fixed с нулевым зазором до комбобокса, модальный ящик 288×914 при вьюпорте 1440×914.
  
  Честная оговорка: прокрутку при открытом попапе в Firefox проверить не удалось — Select там не открывается синтетическим событием, а доверенный клик сам подтягивает элемент к верху. В Chromium этот случай
  проверен отдельно (scrollY: 1500, зазор 0).
  
  cargo make lint-test — чисто, 98 тестов.

---

● Починен пустой drawer после переходов по истории.

## Симптом

После активной навигации по разделам, сжатия и восстановления окна, а затем перехода «назад»,
очередное сжатие показывает мобильную раскладку и гамбургер, но ящик пустой. Замечено в Firefox.

## Причина

Механизм оказался не зависящим от движка — воспроизвёлся и в Chromium:

```
after back           view='desktop' menuKids=6 drawerKids=0
shrunk again 700     view='desktop' menuKids=6 drawerKids=0   ← view не переключился
```

htmx восстанавливает запись истории, подставляя снимок в history-элемент, то есть в `body`.
Элемент `.page`, за которым следил `ResizeObserver`, при этом выбрасывается — проверено прямо
в Firefox: `window.__before === document.querySelector('.page')` после «назад» даёт `false`.

Наблюдатель умирает вместе со старым узлом. А `init_page_element` отказывался инициализировать
новый, потому что маркер `data-initialized` — **атрибут**, то есть часть разметки, и он приезжал
обратно в составе снимка, удостоверяя, что элемент уже настроен.

Дальше всё сходится: CSS живёт без JS, поэтому container query исправно схлопывает раскладку
и показывает гамбургер, а перенос навигации сделать некому — она остаётся в скрытом `.page-menu`,
и ящик пуст.

## Исправление

1. **Маркер инициализации перенесён с атрибута на свойство объекта элемента** (`wgPageInitialized`).
   Свойства не сериализуются в `innerHTML`, поэтому восстановленный узел приходит без маркера,
   инициализируется заново и получает свой `ResizeObserver`.
2. **Положение навигации сверяется на каждом срабатывании**, а не только при смене `view`.
   Раньше `apply_view` выходил досрочно, если атрибут уже совпадал, — а снимок может принести
   навигацию не на той стороне брейкпоинта при уже «правильном» атрибуте. Перенос идемпотентен
   (двигать нечего, если источник пуст), так что лишних движений это не создаёт.

## Как проверялось

Сценарий из отчёта, оба движка.

**Chromium**

```
after back               view='desktop' menuKids=6 drawerKids=0
shrunk again 700         view='mobile'  menuKids=0 drawerKids=6   ← было пусто
restored again           view='desktop' menuKids=6 drawerKids=0
```

**Firefox** (geckodriver) — те же переходы, тот же результат: после «назад» сжатие даёт
`view='mobile'`, `menuKids=0`, `drawerKids=6`.

Механизм подтверждён там же напрямую: узел `.page` после «назад» другой, атрибута-маркера
в разметке нет, свойство-маркер на новом узле выставлено — значит инициализация прошла заново.

**Обратный случай** (снимок снят в мобильном виде, восстановлен на десктопе) — навигация
возвращается в меню, ящик пуст, как и должно быть.

## Попутно закрыт вопрос из прошлой задачи

Оставалась непроверенной работа `container-type: inline-size` в Firefox — не сломает ли
`layout`-containment позиционирование `fixed`, на котором держатся попапы.

- `containerType` в Firefox — `inline-size`, запрос активен;
- попап Select: `position: fixed`, зазор до комбобокса **0px**, привязка цела;
- модальный ящик: 288×914 при вьюпорте 1440×914 — во всю высоту, как задумано.

Прокрутка при открытом попапе в Firefox не проверена: Select там не открывается синтетическим
событием, а доверенный клик сам подтягивает элемент к верху страницы. В Chromium этот случай
проверен отдельно (`scrollY: 1500`, зазор 0).

`cargo make lint-test` — чисто, 98 тестов.

---
