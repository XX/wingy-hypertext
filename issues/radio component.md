Также, как были реализованы компоненты Switch, Select и другие, необходимо перенести компоненты `Radio` и `RadioGroup`, повторяя функционал `wa-radio` и `wa-radio-group` из WebAwesome: `tmp/webawesome/packages/webawesome/src/components/radio` и `.../radio-group`.

Делать после задачи `issues/button group component.md`. Зависимости между ними нет: сегментированный вид радио в WA делается не группой кнопок, а самим радио с `appearance="button"` внутри `RadioGroup`.

Состояние и клавиатура должны работать нативно, как у `Switch`: скрытый `<input type="radio">` внутри `<label>`, отмеченное состояние стилизуется через `:checked`. Браузер сам даёт группировку по `name`, переключение стрелками с переносом, пропуск `disabled` и проверку `required`. Почти вся логика `radio-group.ts` (406 строк) существует из-за shadow DOM и form-associated custom elements — нам она не нужна, так что код в `crates/web`, скорее всего, не понадобится.

`Radio` (`crates/lib/src/component/radio.rs`) — пропы `checked`, `disabled`, `name`, `value`, `appearance` (`Default` | `Button`) и `children` как подпись. Разметка по образцу `Switch`: `<label class="radio …"><input type="radio" class="control" …><span class="label">…</span></label>`. В отличие от WA кружок отмеченного состояния рисуем на CSS, а не вставляем SVG в разметку. Состояния по `name`/`value` несут сами радио, как у нас `SelectOption` несёт `selected` — группа их не проставляет, потому что дети приходят непрозрачным `Renderable`.

`RadioGroup` (`crates/lib/src/component/radio_group.rs`) — пропы `label`, `hint`, `orientation` (`crate::orientation::Orientation`), `size`, `required`, `disabled` и `children`. Разметка — `<fieldset class="radio-group …" role="radiogroup" aria-orientation=…>` с `<legend>` или подписью в `class=LABEL`, детьми и `<small class="hint">`, как у `Switch` и `Input`. Размер задавать классами `size-*`, они наследуются вложенными радио.

Стили — порт `radio.styles.ts` и `radio-group.styles.ts` в `webassets/style/components/radio.css` и `radio_group.css`: обычный вид с кружком, `appearance=button` (высота `--wa-form-control-height`, рамка, фон, скрытый кружок), отмеченное и `disabled` состояния, фокус. Скругление в сегментированном виде: WA расставляет из скрипта атрибуты `data-wa-radio-first` / `inner` / `last`, а у нас плоская разметка, поэтому хватит `:first-child` / `:last-child` и учёта ориентации в CSS. Константы классов — в `crates/lib/src/class.rs`.

Валидацию переносим только нативную (`required` на радио группы). Свой текст ошибки и `setCustomValidity` из WA не повторяем — этого нет и в остальных наших компонентах формы.

Тесты разметки — `crates/lib/src/tests/radio.rs`. Для визуального тестирования нужен раздел Radio Group в `examples/client` (маршрут, пункт меню в разделе Components) с примерами из доки WA (`tmp/webawesome/packages/webawesome/docs/docs/components/radio-group.md`): базовый пример, Initial Value, Hint, Radio Buttons, Disabled, Orientation, Size, Validation.

---

● Задача issues/radio component.md выполнена.
  
  Компоненты. Radio (crates/lib/src/component/radio.rs): пропы appearance (Default / Button), checked, disabled, required, name, value, подпись через children. Разметка по образцу Switch: <label 
  class="radio"><input class="control" type="radio" …><span class="radio-indicator"></span><span class="label">…</span></label>. Кружок выбранного состояния рисуется на CSS, SVG в разметку не вставляется.
  
  RadioGroup (radio_group.rs): label, hint, orientation (по умолчанию Vertical, как в WA), required, disabled. Разметка — <fieldset class="radio-group vertical" role="radiogroup" aria-orientation=…> с <legend 
  class="label">, контейнером .radio-group-items и <small class="hint">.
  
  Кода в crates/web не потребовалось, как и предполагалось: выбор, переключение стрелками и валидация — нативные.
  
  Стили: radio.css и radio_group.css — порт radio.styles.ts и radio-group.styles.ts. Сегментированный вид (appearance=Button) собирается в группе селекторами :first-child / :last-child с учётом ориентации,
  вместо атрибутов data-wa-radio-*, которые WA расставляет из скрипта.
  
  Отступления от задачи. Пропа size у группы нет: размер задаётся классом size-*, как у Input, Select и Switch (в самой задаче это тоже было написано). Валидация только нативная, setCustomValidity не переносил.
  
  Тесты (crates/lib/src/tests/radio.rs): 6 тестов на разметку радио, состояния, сегментированный вид, группу с подписью, подсказкой и детьми. Всего 110 тестов, fmt и clippy (включая wasm-клиент) чистые.
  
  Галерея: раздел Radio Group (маршрут /radio, пункт меню перед Select) с примерами из доки: базовый, Initial Value, Hint, Radio Buttons, Disabled, Orientation, Size, Validation. В демо валидации добавил
  небольшой обработчик в коде галереи — он гасит отправку формы и печатает выбранное значение, иначе страница уходила бы по submit. 
  
  Проверка в Firefox:
  - клик и стрелки переключают выбор нативно (light → medium → обратно с переносом); 
  - disabled на группе: fieldset.disabled=true, все поля в состоянии :disabled, радио с прозрачностью 0.5 и курсором not-allowed;
  - сегментированный вид: радиусы 6/0/0/6, 0/0/0/0, 0/6/6/0, рамки соседей перекрываются на 1px, у выбранного свой фон;
  - валидация: пустая группа не проходит checkValidity, submit блокируется браузером и ничего не печатает; после выбора — Submitted: medium, без перехода со страницы.
  
  По ходу поймал две ошибки в своём CSS, обе исправлены: нулевой gap в сегментированном виде проигрывал по специфичности правилу ориентации (оставался зазор 15px), и звёздочка «обязательно» попадала на подписи
  всех радио, потому что селектор был не про прямого потомка.

---

В WA в вертикально расположенных radio buttons и label, и сами кнопки по ширине растягиваются на всю область группы, а у нас так не происходит. В чём может быть причина?

---

● Причина — правило из нативных стилей, которое перебивало растягивание. Уже исправил.
  
  В чём было дело. В webassets/style/common/native.css есть правило для нативных чекбоксов и радио:

  label:has(input[type='radio']) {
    display: inline-flex;
    width: fit-content;
    ...
  } 
    
  Наш Radio — это как раз <label> с <input type="radio"> внутри, поэтому правило попадало в него и задавало width: fit-content. В колоночном flex-контейнере растягивание (align-items: stretch) работает только
  при width: auto, а fit-content — это явная ширина, так что кнопки сжимались по содержимому.
  
  У WA такого конфликта нет: там радио — это веб-компонент <wa-radio> с shadow DOM, а не метка с input внутри, и правило для нативных элементов на него не распространяется.
  
  Как нашёл. Синтетический тест в странице: пустая метка в колоночном контейнере шириной 300px растягивалась до 300, а наша разметка радио давала 66. Разница только в том, что внутри появляется <input 
  type="radio">, то есть виноват селектор с :has().
  
  Исправление. Одна строка в webassets/style/components/radio.css — у .radio явно задано width: auto с комментарием о причине. Наши стили компонентов не в слое каскада, а нативные лежат в wa-native, поэтому
  правило перебивается надёжно.
  
  После исправления (замеры в Firefox):
  
  ┌──────────────────────────────────────────────┬───────────────┬───────────────┐
  │                                              │     было      │     стало     │
  ├──────────────────────────────────────────────┼───────────────┼───────────────┤
  │ вертикальная группа кнопок (контейнер 300px) │ 72, 70, 70    │ 300, 300, 300 │
  ├──────────────────────────────────────────────┼───────────────┼───────────────┤
  │ вертикальная группа обычных радио (582px)    │ 109, 133, 106 │ 582, 582, 582 │
  └──────────────────────────────────────────────┴───────────────┴───────────────┘
  
  Обычные радио теперь тоже занимают всю ширину группы — как в WA, это заодно увеличивает область клика. Горизонтальные группы не изменились: там ось строк, и ширина по-прежнему по содержимому. Сегментированный
  вид сохранил скругления 6/0/0/6, 0/0/0/0, 0/6/6/0 и перекрытие рамок в 1px, валидация работает как раньше. fmt и 110 тестов проходят.
  
  Тот же нативный селектор есть и для чекбоксов (label:has(input[type='checkbox'])), то есть Switch сейчас тоже получает width: fit-content. Там это пока не мешает, но если понадобится тянуть переключатель на
  всю ширину, причина будет та же.

---

Проверь, нет ли похожей проблемы у Switch

---

● Да, у Switch та же причина, и я её исправил.
  
  Что происходило. Switch рендерит <label class="switch-toggle"> с <input type="checkbox"> внутри, поэтому в него попадало парное правило из native.css:

  label:has(input[type='checkbox']) { display: inline-flex; width: fit-content; … }
  
  В WA этого нет: там внутренняя метка живёт в shadow DOM wa-switch и имеет width: auto, то есть заполняет хост целиком.
  
  Когда это было заметно. В обычной раскладке — никогда, потому что .switch сам по себе inline-flex и сжимается по содержимому. Разница вылезала, как только .switch растягивался. Замеры в Firefox (контейнер
  400px, ширина .switch / ширина .switch-toggle):
  
  ┌───────────────────────────┬───────────┬───────────┐
  │         Контекст          │   было    │   стало   │
  ├───────────────────────────┼───────────┼───────────┤
  │ обычный блок              │ 191 / 191 │ 191 / 191 │
  ├───────────────────────────┼───────────┼───────────┤
  │ колоночный flex-контейнер │ 400 / 191 │ 400 / 400 │
  ├───────────────────────────┼───────────┼───────────┤
  │ .switch с width: 100%     │ 400 / 191 │ 400 / 400 │
  └───────────────────────────┴───────────┴───────────┘
  
  То есть сам переключатель растягивался, а кликабельная строка с подписью оставалась узкой — область нажатия обрывалась на полпути.
  
  Исправление. Одна строка в webassets/style/components/switch.css: width: auto у .switch-toggle с тем же комментарием, что и у .radio.
  
  Проверка. На странице Switch ширины всех примеров совпадают с прежними (191/191, 248/248, 284/284, 210/210, 174/174, 582/582), внешний вид на скриншоте не изменился. fmt и 110 тестов проходят.
  
  Остальные компоненты не затронуты. Метку вокруг поля ввода рендерят только Radio и Switch. У Input и Select подпись — отдельный <label class="label"> рядом с полем, поэтому селектор с :has() в них не
  попадает.

---

Проверь Select и Input на такие же конфликты с native.css

---

● Проверил — у Select и Input такой проблемы нет. Ничего не менял.
  
  Как проверял. Написал в браузере обход всех правил (с раскруткой вложенных селекторов &) и для каждого элемента примеров собрал свойства, которые задаёт только native.css, а наши стили компонента не
  переопределяют. Именно такой была ошибка с width: fit-content.
  
  Инструмент сначала врал, поэтому я его проверил на заведомом случае: убирал наше width: auto у .radio — ширина падала с 300px до 72px, а после удаления native.css менялась снова. То есть метод действительно
  ловит этот класс конфликтов. Попутно выяснилось, что sheet.disabled = true в Firefox на импортированный файл не действует, пришлось удалять само правило импорта.
  
  Что нашлось.
  
  ┌──────────────────────────────────────┬────────────────────┬────────────────────────────────────────────────────────────────────┐
  │               Элемент                │   Из native.css    │                              Опасно?                               │
  ├──────────────────────────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ label.label (подпись Select и Input) │ position: relative │ Нет: внутри только текст, абсолютно позиционированных потомков нет │
  ├──────────────────────────────────────┼────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ input.control (поле ввода Input)     │ width: 100%        │ Нет, наоборот нужно: поле заполняет рамку, как в WA                │
  └──────────────────────────────────────┴────────────────────┴────────────────────────────────────────────────────────────────────┘
  
  Ключевого правила label:has(input[type='radio']) с width: fit-content здесь не возникает: у Select и Input подпись — отдельный <label> рядом с полем, а не обёртка вокруг него. Метку вокруг поля рендерят
  только Radio и Switch, и их я уже поправил.
  
  Проверил и поведением, в контейнере шириной 400px: 
  
  ┌──────────────────────────────┬──────────────┬─────────────────┐
  │                              │ обычный блок │ колоночный flex │
  ├──────────────────────────────┼──────────────┼─────────────────┤
  │ Input (.input / .text-field) │ 400 / 400    │ 400 / 400       │
  ├──────────────────────────────┼──────────────┼─────────────────┤
  │ Select (.select / .combobox) │ 400 / 400    │ 400 / 400       │
  └──────────────────────────────┴──────────────┴─────────────────┘
  
  Оба растягиваются целиком, внутренние поля тоже: 366px и 334px — это ширина за вычетом отступов и иконок.
  
  Заодно проверил легенду группы радио: у неё нативный legend { padding-inline } перебивается нашим padding: 0, вычисленные отступы нулевые.
  
  В рабочем дереве с прошлого раза ничего нового не появилось: правка switch.css от предыдущей задачи и файлы Radio. Ничего не закоммичено.

---

